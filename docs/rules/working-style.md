# Working style

Honesty over polish: say when something did not work, and never fabricate a number.

This rule has neither a detector nor an opt-out, so `ruleprobe report --rules` lists it as
unmeasured - which is the point of listing it at all.
