---
bmad_id: "RP-T004"
type: "task"
title: "chore(release): re-derive the harness surface and cut 0.2.0"
lifecycle: "active"
provenance: "authored"
github_issue: 58
github_issue_url: "https://github.com/JakeSelby/ruleprobe/issues/58"
parent_bmad_id: "RP-E008"
parent_github_issue: 27
updated: "2026-09-24"
---

# RP-T004 — chore(release): re-derive the harness surface and cut 0.2.0

<!-- bmad-sync:begin -->
- **GitHub issue:** [#58](https://github.com/JakeSelby/ruleprobe/issues/58)
- **Primary parent:** [RP-E008](https://github.com/JakeSelby/ruleprobe/issues/27)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Goal

Cut ruleprobe 0.2.0 with a contract that matches what agent-harness imports on the day it ships, so
the harness bumps its pin with no code change (agent-harness AD-13). It runs last in the release,
after every in-scope item in Epics 1 to 5, RP-D010, and story 4.3 if the maintainer confirms it.
Binds FR-30 Declared public API, FR-31 Versioning policy, and AD-9 The contract is versioned, once,
in 0.2.0.

## Acceptance criteria

1. **Given** agent-harness `main` on the release day, **when** its ruleprobe imports, call shapes and
   non-name dependencies are re-derived, **then** the contract test is updated in the same change and
   passes, and the harness commit read is recorded.
2. **Given** `CHANGELOG.md`, **when** the version section is cut, **then** it names the 0.2 break
   (story 1.9).
3. **Given** `scripts/release_preflight.py`, **when** run in a fresh clone, **then** it is clean, and
   the wheel is published by the release workflow.

## Tasks

- [x] Re-derive the harness surface from harness `main` (AC: 1)
  - [x] List root imports, submodule imports, call shapes and `ruleprobe.__file__` use, as FR-30
        lists them from `1307113`
  - [x] Update `tests/test_contract.py` and the README's public API section in the same change
  - [x] Record the harness commit read
- [x] Cut the 0.2.0 version section in `CHANGELOG.md`, naming the break (AC: 2)
- [x] Bump the version and follow `docs/releasing.md` (AC: 3)
  - [x] Run the full suite and the corpus floor
  - [x] Run `scripts/release_preflight.py` in a fresh clone (2026-09-24: `release preflight: 0.2.0 is ready`)
  - [x] Confirm the release workflow published the wheel (2026-09-24: the `v0.2.0` run's four jobs
        passed; PyPI serves 0.2.0, `uvx ruleprobe@0.2.0 --version` prints `ruleprobe 0.2.0`,
        `advance_stable.py --check` puts `stable` at `v0.2.0` (`ca93e24`), and the GitHub release
        carries the wheel and the sdist)

## Dev notes

Files to touch: `tests/test_contract.py` (created by story 1.8), `CHANGELOG.md`, `README.md`,
`docs/releasing.md`, and the version. epics.md names `pyproject.toml` for the version, but
`pyproject.toml` reads it dynamically from `ruleprobe.__version__`, which is `"0.1.0"` in
`ruleprobe/__init__.py` today, so the bump lands there.

The contract test must cover every FR-30 name and call shape, including `fn(events, ctx)` reading
`ctx.bash`, `ctx.finals` and `Parsed.event`. AD-9's non-name dependencies also hold: `SECRET_PATTERNS`
stays a literal list in `ruleprobe/detectors/common.py`; the wheel is
`ruleprobe-<version>-py3-none-any.whl`, carries `corpus/`, and imports from a zip. AD-8's
`dependencies = []` must still hold.

Testing: full suite, corpus floor, preflight.

- [Source: _bmad-output/planning-artifacts/epics.md#story-62]
- [Source: _bmad-output/planning-artifacts/prds/prd-ruleprobe-2026-09-23/prd.md#fr-30-declared-public-api]
- [Source: _bmad-output/planning-artifacts/prds/prd-ruleprobe-2026-09-23/prd.md#fr-31-versioning-policy]
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-ruleprobe-2026-09-23/ARCHITECTURE-SPINE.md#ad-9]
- [Source: docs/releasing.md#cutting-it]

## Dev agent record

### Agent model used

Claude Opus 5.5 (`claude-opus-5-5`).

### Completion notes

The first task group (AC 1) is done. The changelog fold and the version bump (AC 2 and AC 3)
landed in the release step, run by the orchestrator once every other in-scope item had merged
(2026-09-24): `## Unreleased` became `## 0.2.0 (2026-09-24)` above a fresh empty one, its
`### Breaking` lead names the seven-part break at the top, a `### Measured` section after
`### Fixed` carries the field precision (#53) and the sixty-second timing (#50), and
`__version__` is `0.2.0`. `release_preflight.py --tag v0.2.0` reports it ready.

**Maintainer decisions, 2026-09-24.**

- The agent-harness bump updates its corpus-count test: 0.2.0's shipped corpus labels eleven
  detector ids, not six, so AC 1's "bump with no code change" goal is set aside for that one
  test (Epic 6, #27, which moves to v0.3.0 since it follows the tag).
- The release notes publish the field precision, labelled as agent-judged on one developer's
  transcripts, and the timing on the synthetic reference volume.
- Defaults confirmed: no model on Gemini events, a Gemini write the user edited not measured
  as a write, whole-file-cat's redirected group as a follow-up, and the Gemini reader's
  build-time defaults.
- #67, the reference site, moved to the v0.3.0 milestone so this one can empty.

**Harness commit read:** agent-harness `main` at `40cc0b7769a21f811382f8d5f9b6347565980f5a`
(2026-09-24). Files that touch ruleprobe: `policy/hooks/rule-detectors.py`,
`policy/hooks/decisions.py`, `policy/hooks/usage-log.py` (through the rule pack),
`scripts/detector_corpus.py`, `bin/harness` (through the rule pack), and the tests
`test_rule_detectors.py`, `test_rule_pack_golden.py`, `test_detector_corpus.py`,
`test_doc_figures_derive_from_code.py` and `test_allow_sampling.py`.

**The derived surface.**

- Imports: root `Registry`, `analyse`, `counts`, `run`; `ruleprobe.detectors.common` (as a
  module: `DETECTORS`, `SECRET_PATTERNS`, `__file__`); `ruleprobe.events` `hit`, `input_of`,
  `text_of`; `ruleprobe.registry` `Detector`, `Registry`; `ruleprobe.shell` `MAX_COMMAND`,
  `MARKER_RE`, `SUB_PLACEHOLDER`, `git_calls`, `has_redirect`, `normalise`, `operands`,
  `pipelines`, `strip_heredocs`; `ruleprobe.declarative` `load`; `ruleprobe.validity` `Score`,
  `CorpusError`, `DEFAULT_FLOOR`, `below_floor`, `score_corpus`, `scores_as_dict`,
  `validity_table`; `ruleprobe.__file__`. The same set as FR-30.
- Call shapes: `fn(events, ctx)` returning `(turn, tool_use_id)` tuples; `ctx.bash`,
  `ctx.finals`; `Parsed.event`, `.command`, `.heredocs`, and in the harness tests `.skipped` and
  `.pipelines`; `analyse(events).bash`; `MARKER_RE.match(v).group(1)`; `hit(e)`,
  `hit(e, tool_use_id=False)`; `git_calls(parsed, ("commit",))` 3-tuples; `pipelines`,
  `operands`, `strip_heredocs`, `normalise` with their exact nested shapes; `Detector` built with
  four or five positional arguments, subclassed with `__slots__ = ()` and `kind`/`stance`
  properties, `.id .rule .event .fn .gate` read on `common.DETECTORS`; `Registry(list)`;
  `run(events, stances, registry=, strict=, errors=)` returning `{id: [hit]}` with empty
  detectors omitted, each hit a 3-sequence `(id, turn, tool_use_id)`, and `errors` entries stored
  in ledger rows; `counts(events)` with keys `web_search`, `agent`, `ask_user`;
  `score_corpus(registry=, directory=)`; `Score(id).add`, `.scored`, `.precision`, `.recall`,
  `.detector`; the three floor helpers positional; `scores_as_dict` extended and dumped as JSON;
  `declarative.load(path)` returning `(document, lines)`.
- Non-name dependencies: `SECRET_PATTERNS` a plain `ast.Assign` of a literal list, read out of the
  wheel member `ruleprobe/detectors/common.py` and each pattern `re.compile`d; the wheel
  `ruleprobe-<version>-py3-none-any.whl` on `sys.path` as a zip; the corpus at
  `ruleprobe/corpus/`, found by walking up from `ruleprobe.__file__` to the zip and extracting;
  detector `event` values `bash`, `write`, `agent-brief`, `assistant-final`, `session` (five of
  the seven `Registry.add` accepts);
  `common.DETECTORS` exactly the six generic ids; the harness's own corpus in the scorer's layout
  with an extra top-level `known_below_floor`; the shipped `labels.yaml` read by pattern as
  `fire:`/`near:` flow lists, and (harness-side) its labelled ids asserted equal to the six
  generic ids. ruleprobe's contract test asserts only that the six are a subset of them.

**Differences from FR-30** (taken from `1307113`): no import name added or removed. FR-30 did not
list the items below. Each is named in the README's public API section and held by the test
named, all in `tests/test_contract.py` unless stated:

- `Detector` with the gate omitted, re-registering the shipped six ungated under a subclass:
  `test_four_positional_arguments_and_a_re_registered_shipped_detector`.
- A subclass with `kind` and `stance` properties: `test_positional_detector_and_a_slots_subclass`.
- The `event` values (all seven accepted, another refused):
  `test_the_event_kinds_a_consumer_registers_under`.
- `run`'s `{id: [hit]}` return, empty detectors omitted, 3-field hits:
  `test_run_returns_three_field_hits_keyed_by_id_and_omits_the_empty`; the six over a
  caller-built session, each with a hit, `secret-in-write` and `model-switch` included:
  `test_the_shipped_detectors_over_a_caller_built_session`.
- `Parsed.event` identity per element: `test_analyse_returns_the_context_run_uses`;
  `Parsed.skipped` and `.pipelines`: `test_parsed_skipped_and_pipelines`; the shell helpers'
  nested shapes: `test_the_shell_helpers_return_the_nested_shapes`.
- The six generic ids: `test_the_shipped_detectors_are_the_six_generic_ones`.
- `counts` keys `web_search`, `agent`, `ask_user`: `test_counts_is_a_dict_of_ints`.
- `errors` entries `{"detector", "error"}`, as stored in ledger rows:
  `test_errors_collects_a_raising_detector_and_strict_raises_it`.
- `Score(id).add`, `.scored`, `.precision`, `.recall`, `.detector`: `test_score_corpus_and_score`;
  the three floor helpers positional: `test_the_floor_helpers_take_scores_and_floor_positionally`.
- A consumer corpus scored and `scores_as_dict` extended into JSON:
  `test_score_corpus_over_a_consumer_corpus`.
- `SECRET_PATTERNS` compiled: `test_every_secret_pattern_compiles`; read from the wheel member:
  `test_secret_patterns_read_from_the_wheel_member`.
- The shipped labels as flow lists, a `- fire:` block list and `misfire:` near-misses included,
  and the six generic ids a subset of the labelled ids: `test_every_fire_and_near_is_a_flow_list`.

Held elsewhere: AD-8 `dependencies = []` by `tests/test_envelope.py`
(`test_pyproject_declares_no_runtime_dependency`); the zip import and the corpus beside `__file__`
by `test_the_package_imports_and_runs_from_the_zip` and `test_the_corpus_is_in_the_wheel_beside_file`
here, and on the built wheel by CI's `package` job, which passes it in as `RULEPROBE_WHEEL`.

**Open decision, for the maintainer.** 0.2.0's shipped corpus labels eleven ids: the six generic
detectors and the catalog's five. The harness's
`test_the_docs_say_how_many_detectors_the_two_corpora_score` asserts the wheel's labels name
exactly the six, so it fails on 0.2.0 and needs a change in the harness's bump. AC 1's goal, a
bump with no code change, therefore does not hold as things stand. ruleprobe keeps its check as
the subset assertion. The choice is between that harness change and shipping fewer labels.

Command run against a wheel built from this tree (`_source_wheel`, placed at the harness's pinned
path `lib/vendor/ruleprobe-0.1.0-py3-none-any.whl` in a `git archive 40cc0b7` export), from the
export's root:

```sh
python3 -m unittest tests.test_rule_detectors tests.test_rule_pack_golden \
  tests.test_detector_corpus tests.test_doc_figures_derive_from_code tests.test_allow_sampling \
  tests.test_brief_cap_metric tests.test_brief_guard tests.test_commit_msg_hook \
  tests.test_brief_budget
```

Result: `Ran 165 tests`, `FAILED (failures=1, errors=1)`. The failure is the test above. The
error, `test_expected_modules_is_the_glob_at_the_task_s_parent_sha`, needs git history the export
lacks and errors the same way on the vendored 0.1.0 wheel. `scripts/detector_corpus.py --floor 0.9`
exits 0 on the same export.

**Verification.** `python3 -m unittest discover -s tests -p test_contract.py`: `Ran 45 tests`,
`OK`. The repository gate, `.agent-harness/tools/gate.sh` on the staged tree:

```text
compileall: exit 0
unittest: exit 0
unittest39: exit 0
corpus: exit 0
audit: exit 0
audit_delivery: exit 0
preflight: exit 0
unittest: Ran 1149 tests in 4.912s OK
unittest39: Ran 1149 tests in 6.083s OK
python39: 3.9.25
corpus: total                                    87  110   87    0    0   1.00    1.00   1.00  floor 0.90
audit: audit: 58 issue(s), 0 finding(s)
audit_delivery: depth: issue #58, 0 finding(s)
preflight: release preflight: 0.1.0 is ready
```

### File list

- `tests/test_contract.py`
- `README.md`
- `_bmad-output/implementation-artifacts/RP-T004.md`

## Change log

- 2026-09-23: written from the planning corpus before implementation.
- 2026-09-24: harness surface re-derived at `40cc0b7`; contract test and README updated.
- 2026-09-24: release step: changelog folded into 0.2.0 with a Measured section, `__version__` 0.2.0,
  the maintainer's release decisions recorded; preflight ready for `v0.2.0`.
- 2026-09-24: tagged `v0.2.0` on `ca93e24`; the release workflow's publication verified (#109).
