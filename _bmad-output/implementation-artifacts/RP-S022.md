---
bmad_id: "RP-S022"
type: "story"
title: "feat(rules): bind rules per sentence"
lifecycle: "active"
provenance: "authored"
github_issue: 141
github_issue_url: "https://github.com/JakeSelby/ruleprobe/issues/141"
parent_bmad_id: "RP-E010"
parent_github_issue: 128
updated: "2026-09-25"
---

# RP-S022 — feat(rules): bind rules per sentence

<!-- bmad-sync:begin -->
- **GitHub issue:** [#141](https://github.com/JakeSelby/ruleprobe/issues/141)
- **Primary parent:** [RP-E010](https://github.com/JakeSelby/ruleprobe/issues/128)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As a developer with dense rule files,
I want each sentence to bind its own detector,
so that a section mixing several rules is measured rather than dropped.

## Context and value

The 0.2.0 binder reads a section as one unit: two matching sentences, or one exception word
anywhere, leave the whole section unmeasured. On the rules zoo it measures recall 0.44 (18/41) with
no false bind (story 8.2, #139). RP-SP004 (#137) measured a per-sentence prototype at 0.73 recall
with five false binds, every one an exception stated in the sentence after the rule, and variant B,
which lets that next sentence unbind, at 0.68 with one false bind, the "exempt" gap 8.2 closed. Its
decision, taken under the maintainer's delegation, is that this story builds variant B, and that the
0.70 recall bar moves to Epic 8 as a whole, since 9 of the 11 misses are catalog phrasings (8.4).

## Acceptance criteria

1. **Given** a section whose two sentences each match a different catalog entry, **when** it binds,
   **then** it binds both, and the rule id is still AD-12's section id.
2. **Given** a sentence matching no entry or several, **when** it binds, **then** that sentence binds
   nothing.
3. **Given** "unless" in one sentence, **when** the section binds, **then** only that sentence, and
   the matched sentence right before it, are unbound.
4. **Given** an exception or permission in the sentence right after a rule, **when** the section
   binds, **then** that rule is unbound; **given** a condition or contrast word, **then** it unbinds
   only the sentence it is in.
5. **Given** "admit no exception" or "without exception", **when** the sentence binds, **then** the
   marker does not unbind it.
6. **Given** the zoo, **when** `corpus` runs, **then** it shows zero false binds, and the recorded
   recall floor is the recall measured.
7. **Given** a section several detectors measure, **when** `report` prints the coverage block,
   **then** its line lists them all.

## Design

### Approach

- `rules._match` returns one `(detectors, marker)` pair per sentence, heading first, over the
  existing `_sentences` split. A sentence's marker is, in order: an exception or permission word
  (`_EXCEPTION`) or a condition or contrast word (`_CONDITION`) in the sentence itself; an
  exception or permission word in the next sentence of the section; an exception or permission
  word in the heading. Both word lists read the sentence after the catalog's negated markers are
  removed.
- `rules._bound` keeps each sentence that matched exactly one live entry and has no marker, and
  returns those entries in catalog order, once each. `_bind` makes the rule measured and
  catalog-bound to all of them; with none, it names the first unbinding word, else the first
  sentence's several matches (the reasons 0.2.0 printed), else the caller's reason.
- `catalog.NEGATED_EXCEPTIONS` is the closed list, as plain phrases in the literals-only catalog
  module; `rules._NEGATED` compiles it longest first, whitespace-tolerant, singular or plural.
- A heading-less file, or a headed file with no rule section, binds per sentence the same way.
- `validity.BINDING_RECALL_FLOOR` rises from 0.43 to 0.68.
- No output shape changes: `RuleEntry.detectors` was already a list, the coverage line already
  joins it, and `report --json` carries counts only. Nothing goes under Breaking.

### Decisions and alternatives

1. **Variant B's scope** (RP-SP004's decision): an exception or permission in the next sentence
   unbinds. Rejected: own-sentence scope, five false binds on the zoo; section-wide scope, the 0.2.0
   binder at 0.44 recall; the refer-back refinement, which waits for held-out near-misses.
2. **A heading's exception or permission unbinds the whole section.** Variant B read literally
   would reach only the first sentence under it; the heading qualifies everything beneath, so 0.2.0's
   behaviour is kept there. It changes no zoo figure. Flagged to the maintainer to confirm.
3. **Detectors in catalog order, deduplicated,** not sentence order, so reordering a section's
   sentences never changes its report line.
4. **A mixed-source rule stays one entry.** A user detector replacing one bound id makes the whole
   rule `own`, and a bound id the registry no longer holds leaves the whole rule unmeasured, as
   `_sourced` already does; both under-count. Rejected: splitting a section into one entry per
   detector, which would change AD-12's rule id.
5. **The negated markers live in the catalog module** as AD-18 says, as phrases rather than a
   regular expression, so the module stays literals only and the list stays readable.

### Do not implement

- Wider catalog patterns or patterns for the four uncatalogued defaults (story 8.4).
- The refer-back exception refinement (RP-SP004 decision 3).
- Any zoo label change, or a looser false-bind gate.

## Tasks

- [x] Per-sentence `_match`, `_bound`, `_bind` and `_catalog_matches` in `ruleprobe/rules.py` (AC: 1–5)
- [x] `NEGATED_EXCEPTIONS` in `ruleprobe/detectors/catalog.py` and `_NEGATED` in `rules.py` (AC: 5)
- [x] Raise `BINDING_RECALL_FLOOR` to 0.68 and update the shipped-binding JSON test (AC: 6)
- [x] Tests in `tests/test_catalog.py` and `tests/test_rules.py`, one per spike near-miss (AC: 1–7)
- [x] Amend AD-18 through `bmad-architecture` (update intent) with memlog entries
- [x] README binding text and quoted `corpus` output; CHANGELOG under Unreleased

## Dev notes

### Bound decisions and requirements

- FR-36 (per-sentence binding), NFR-4 (under-count), AD-18 (rules bind per sentence), AD-12 (the
  section rule and its id), AD-4 (under-count rather than over-count), RP-SP004's decision.
- FR-36's consequence "unbinds only its own sentence" and FR-37's "ships only at recall ≥ 0.70"
  now differ from the build; the PRD amendment is left to the maintainer (see Completion notes).

### Files to touch

- `ruleprobe/rules.py`: the binder and the module docstring
- `ruleprobe/detectors/catalog.py`: `NEGATED_EXCEPTIONS`
- `ruleprobe/validity.py`: `BINDING_RECALL_FLOOR`
- `tests/test_catalog.py`, `tests/test_rules.py`, `tests/test_validity.py`
- `README.md`, `CHANGELOG.md`
- The architecture spine and its memlog

### Testing

- `test_catalog.BindingTests`: two sentences bind two entries under the section id; a sentence
  matching two entries (a patched catalog) binds nothing while another still binds; "unless"
  unbinds its sentence and the one before only; an exception two sentences on leaves the rule
  bound; a heading's permission unbinds the section; a condition unbinds only its sentence; every
  negated marker binds and near-misses of them do not; each near-miss RP-SP004 named (`t05`,
  `t06`, `h05`, `f04`, `x01`, `x02`, `x04`, `x05`, `t11`, `t12`, `f10`, `p08`, `m06`, `t07`, `f06`)
  binds as decided.
- `test_catalog.HostileInputTests`: `_NEGATED` and `_match` stay fast on hostile text; the marker
  list is plain lower-case phrases.
- `test_rules.SentenceBindingTests` and `CliTests`: catalog order whatever the sentence order, one
  entry bound twice listed once, every bound detector in the registry, a user replacement makes the
  rule `own`, a heading-less file binds per sentence, deterministic loads, and the report line lists
  all three detectors while `--json` counts one measured rule.
- The zoo gate: `ShippedBindingTests` and `corpus --floor 0.9`.

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 8.3]
- [Source: _bmad-output/implementation-artifacts/RP-SP004.md#Result]
- [Source: _bmad-output/implementation-artifacts/RP-SP004.md#Decision]
- [Source: _bmad-output/planning-artifacts/prds/prd-ruleprobe-2026-09-23/prd.md#FR-36]
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-ruleprobe-2026-09-23/ARCHITECTURE-SPINE.md#AD-18]
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-ruleprobe-2026-09-23/ARCHITECTURE-SPINE.md#AD-12]
- [Source: ruleprobe/rules.py#_match]

## Dev agent record

- **Status:** in-review: committed locally on `feat/per-sentence-binding`, not pushed.
- **baseline_commit:** `dcd0aeb851dc947d3b5fafbbcab109bc6b93a711`

### Agent model used

Claude Opus 5.5 (builder).

### Completion notes

- The zoo, validated and synthetic, not field accuracy: recall 0.68 (28/41), precision 1.00
  (28/28), no false bind, section level; 28 sentence binds and no sentence-level false bind, counted
  with RP-SP004's sentence-level method. Per entry the figures equal RP-SP004's variant B row, less
  `t12`. The recall floor is 0.68.
- Deviation: a heading's exception or permission unbinds the whole section (decision 2), kept from
  0.2.0; no zoo figure moves.
- Needs the maintainer: PRD FR-36 and FR-37 still say own-sentence scope and a 0.70 binder bar; they
  want a `bmad-prd` update-intent amendment, not made here.
- Implemented directly from this story, with no builder subagent; the spine's reviewer lenses were
  not run for the same reason (lint 0 findings).

### File list

- `ruleprobe/rules.py`
- `ruleprobe/detectors/catalog.py`
- `ruleprobe/validity.py`
- `tests/test_catalog.py`
- `tests/test_rules.py`
- `tests/test_validity.py`
- `README.md`
- `CHANGELOG.md`
- `_bmad-output/planning-artifacts/architecture-spines/architecture-ruleprobe-2026-09-23/ARCHITECTURE-SPINE.md`
- `_bmad-output/planning-artifacts/architecture-spines/architecture-ruleprobe-2026-09-23/.memlog.md`
- `_bmad-output/issue-map.json`
- `_bmad-output/implementation-artifacts/RP-S022.md` (new)

## Review findings

- None yet: no review has run on this change.

## Change log

- 2026-09-25: written at build time from story 8.3 and RP-SP004's decision; built and committed locally.
