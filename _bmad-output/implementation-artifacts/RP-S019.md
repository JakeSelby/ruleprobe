---
bmad_id: "RP-S019"
type: "story"
title: "feat(readers): read the third runtime's transcripts into the event schema"
lifecycle: "active"
provenance: "authored"
github_issue: 55
github_issue_url: "https://github.com/JakeSelby/ruleprobe/issues/55"
parent_bmad_id: "RP-E007"
parent_github_issue: 26
updated: "2026-09-23"
---

# RP-S019 — feat(readers): read the third runtime's transcripts into the event schema

<!-- bmad-sync:begin -->
- **GitHub issue:** [#55](https://github.com/JakeSelby/ruleprobe/issues/55)
- **Primary parent:** [RP-E007](https://github.com/JakeSelby/ruleprobe/issues/26)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As a developer on the third runtime,
I want `ruleprobe report` to read my sessions,
so that the same detectors measure my rules.

## Context and value

ruleprobe reads two runtimes today: `RUNTIMES` in `ruleprobe/readers/__init__.py` holds
`claude-code` and `codex`. Runtime-neutral is a claim until a third reader lands (maintainer decision
5). FR-4 already fixes how a runtime is added: one module with `ROOT`, `transcripts(root)`,
`read(path)` and one `RUNTIMES` entry. The runtime itself is chosen by RP-SP003.

## Acceptance criteria

1. **Given** `--runtime <name>`, **when** run, **then** that runtime's sessions are read, and `auto`
   includes them.
2. **Given** its shell tool, **when** read, **then** it reaches detectors as `Bash` with `command`;
   file tools map to the canonical names or stay native, never lossily.
3. **Given** a malformed line, an unreadable file and a missing `ROOT`, **when** read, **then** the
   line is skipped, the file lands in `errors`, and the missing root yields nothing and no error.
4. **Given** `turn` and `final`, **when** derived, **then** they follow AD-2 exactly as the other
   readers do.
5. **Given** the reader lands, **then** at least one labelled synthetic session from the runtime is in
   the corpus, and the README names the runtime and what its transcripts do not record.

## Design

### Approach

One new module in `ruleprobe/readers/` that imports `ruleprobe.events` only, with `ROOT`,
`transcripts(root)` returning sorted paths, and `read(path)` returning a `Session`, plus one
`RUNTIMES` entry (AD-10). It owns one module-level tool-name table, as `ruleprobe/readers/codex.py`
maps `exec_command` to `Bash`, and keeps the native key beside the canonical one (AD-3). `turn`
increments on every `user_prompt`; `final` is the last `assistant_text` before the next
`user_prompt` or the end (AD-2). The module takes its place in the dependency graph and the spine is
updated in the same change (AD-1).

### Decisions and alternatives

- The runtime is decided in RP-SP003, not here.
- A native tool with no exact canonical twin keeps its native name and under-counts, rather than a
  lossy mapping that gains a hit (AD-3, AD-4).

### Do not implement

- No new event kind or field unless added to `events.py` first in this change (AD-2).
- No detector that branches on the runtime.
- Full near-miss coverage at the floor is RP-T003; this story lands one labelled session.

## Tasks

- [ ] Add the reader module with `ROOT`, `transcripts`, `read` and the tool table (AC: 1, 2)
- [ ] Register it in `RUNTIMES` so `--runtime` and `auto` include it (AC: 1)
- [ ] Handle malformed lines, unreadable files and a missing root (AC: 3)
- [ ] Derive `turn` and `final` per AD-2 (AC: 4)
- [ ] Add fixtures and one labelled synthetic corpus session (AC: 5)
- [ ] Add the README note and update the spine's dependency graph (AC: 5)

## Dev notes

### Bound decisions and requirements

- FR-32: A third reader, Cursor or Gemini CLI.
- FR-3: Every reader emits the five event kinds with the fields `ruleprobe/events.py` documents.
- FR-4: A runtime is added by one module with `ROOT`, `transcripts(root)`, `read(path)` and one
  `RUNTIMES` entry.
- AD-1: Dependency direction. AD-2: The event schema is the one contract between readers and
  detectors. AD-3: One tool vocabulary across runtimes. AD-10: How a reader is added.
- Depends on RP-SP003.

### Files to touch

- `ruleprobe/readers/<runtime>.py` (new)
- `ruleprobe/readers/__init__.py` (`RUNTIMES`)
- `ruleprobe/corpus/sessions/`
- `ruleprobe/corpus/labels.yaml`
- `tests/fixtures/`
- `README.md`

### Testing

- `tests/test_readers.py`: runtime selection, tool mapping, error behaviour, `turn` and `final`
  (AC: 1 to 4).
- `tests/test_validity.py`: the labelled session scores (AC: 5).

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 5.2]
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-ruleprobe-2026-09-23/ARCHITECTURE-SPINE.md#AD-2]
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-ruleprobe-2026-09-23/ARCHITECTURE-SPINE.md#AD-3]
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-ruleprobe-2026-09-23/ARCHITECTURE-SPINE.md#AD-10]
- [Source: ruleprobe/readers/__init__.py#RUNTIMES]

## Dev agent record

### Agent model used

Not started.

### Completion notes

Not started.

### File list

Not started.

## Review findings

None yet.

## Change log

- 2026-09-23: written from the planning corpus before implementation.
