---
epic: 4
date: 2026-09-24
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 4 retrospective: Every hit explains itself, and a wrong hit becomes a label

GitHub epic [#25](https://github.com/JakeSelby/ruleprobe/issues/25), BMad ID RP-E006. Written by
`bmad-retrospective` in headless mode; nothing here was confirmed by a person. The Assumptions
section lists every choice made without one.

## Epic summary

- **Epic.** Epic 4 of `_bmad-output/planning-artifacts/epics.md` ("Every hit explains itself, and
  a wrong hit becomes a label", line 634): a developer can see the event and detector behind any
  hit, and can turn a false positive into a labelled negative with one command. Exit criteria are
  declared in `RP-E006.md`.
- **Stories completed (3 of 3).**
  - Story 4.1 (#51, RP-S017): `ruleprobe explain`, merged as PR #82.
  - Story 4.2 (#52, RP-S018): `ruleprobe label`, merged as PR #84.
  - Spike 4.3 (#53, RP-SP002): field precision per shipped detector, merged as PR #105.

  The spike's Result led to four fixes outside the epic's own children, each with its own issue
  and story: #86 (RP-B003, PR #92), #87 (RP-B004, PR #95), #88 (RP-B005, PR #104) and #98
  (RP-B007, PR #100). They are read here as the spike's consequences, not as Epic 4 stories.
- **Sprint-status repair made by this run.** `detect-epic --epic 4` first returned
  `story_count: 3` and `pending_stories: ["4-3-spike-validity-field-precision-per-shipped-detector-from-han"]`,
  whose status was `ready-for-dev`. The spike merged as `fdfefe0` (`Closes #53`), so this run set
  it `done` with `bmad-sprint-planning`'s `sprint_plan.py generate --set <key>=done`, as the
  Epic 3 and Epic 5 retrospectives did. The dry run reported `in_sync: true` and `changed: 0`, and
  the write touched only that key and `last_updated`. A second `detect-epic --epic 4` returned
  `pending_stories: []`.
- **Diff range.** The envelope is `ea82257^..fdfefe0` (18 commits, no merges). Eleven commits
  inside it belong to Epics 2, 3 and 5, to other bug fixes and to four earlier retrospectives, and
  are excluded. The epic and the spike's fixes are these seven squash commits:

  | Commit | PR | Item |
  | --- | --- | --- |
  | `ea82257` | #82 | #51 RP-S017 (4.1) |
  | `ecacae5` | #84 | #52 RP-S018 (4.2) |
  | `fdfefe0` | #105 | #53 RP-SP002 (4.3) |
  | `0d8bc4b` | #92 | #86 RP-B003 (spike fix) |
  | `5464b32` | #95 | #87 RP-B004 (spike fix) |
  | `3028d3a` | #100 | #98 RP-B007 (spike fix) |
  | `560e60f` | #104 | #88 RP-B005 (spike fix) |

  `git_evidence.py --range ea82257^..fdfefe0 --stories 82,84,105,92,95,104,100` attributed each
  of the seven to its PR number and none of the other eleven. Change volume, from
  `git show --numstat`: the three epic commits make 21 file changes across 16 distinct files,
  3,331 lines added and 56 deleted; the four fixes add 2,822 and delete 259 over 46 file changes.
- **Evidence inventory.**
  - Available: the epic spec and `RP-E006.md` with declared exit criteria; the three story files
    and the four fix stories, with their review records where they have one; the seven commits
    and their messages; `sprint-status.yaml`; `issue-map.json`; the PRD and the architecture
    spine with its memlog; `deferred-work.md`; the Epic 1, 2, 3 and 5 retrospectives.
  - Not used: the pull requests themselves. This run used local evidence only, so each PR is read
    through its squash commit and its story file. Local orchestrator state outside the tracked
    files was not read.
  - Missing: session logs. Process lessons rest on the story files and the commit history only.
    The spike's sample sheets and verdicts stay on the measuring machine by design (RP-SP002,
    "Evidence"), so its figures are read as recorded, not re-derived.

## Findings

Each finding carries its source and two dispositions: what to do about this instance, and what
would prevent the next one.

### Aggregate views

**Architecture delta.**

- F1. `ecacae5` (RP-S018) added two top-level imports to `cli` that the AD-1 graph does not hold:
  `from .declarative import ...` and `from .detectors.common import REDACTED, SECRET_PATTERNS,
  redact` (`ruleprobe/cli.py` lines 39 and 40). The graph lists `cli`'s edges as `report`,
  `registry`, `rules`, `validity` and `readers` only (`ARCHITECTURE-SPINE.md`, AD-1 flowchart),
  and `ecacae5` changed one spine line, AD-13's heading, not AD-1. `cli` also imports the private
  `report._redact` (`cli.py` line 44). RP-S017's own build review had patched the same edge
  away: "`cli` imported `detectors.common` against AD-1 (it redacts through `report`)" (RP-S017,
  Review findings). Both edges point down, so there is no cycle, but the graph is no longer the
  whole picture, and the story that reintroduced one edge does not say why. *Instance:* fix now:
  either amend AD-1's graph through `bmad-architecture` update, or route `label`'s redaction and
  emitting through `report` and `validity`. *Prevention:* F9, a test that holds the graph.

**Duplication map.** Redaction has one implementation, `detectors.common.redact`, as AD-13
requires. `report._redact` wraps it with escaping, and `label` calls both. The two call paths
share the function, not a copy of it.

- No finding.

**Size growth.** `ruleprobe/cli.py` was 265 lines before the epic (`ea82257^`) and is 874 now;
`ecacae5` alone added 481, `label`'s selection, refusal, write, read-back and rollback machinery.
`explain`'s logic sits in `report` as library functions (`report.explain`, `explain_text`,
`explain_row`), while `label`'s sits in `cli` with no library entry point. The new test files are
`tests/test_explain.py` (749 lines) and `tests/test_label.py` (798).

- F2. `cli` is becoming the home of `label`'s corpus-writing logic, which a library caller cannot
  reach. *Instance:* defer; nothing in the PRD asks for a library `label`. *Prevention:* none
  yet; see Open questions.

**Pattern divergence.**

- F3. RP-S018's Design decision 23 says a Claude Code subagent's transcript carries its parent's
  session id, so label reruns the detector over every session at one address and takes the one
  with a hit (RP-S018 line 155). `0d8bc4b` (#86) made a subagent its own session,
  `<parent sessionId>/<agentId>` (RP-B003, AC 1 and Design 4), and `5464b32` (#87) made copies
  one session. `_labelled_event` now takes the first session at the address, and names set-aside
  copies on a miss (`cli.py` lines 561 to 594). RP-S018 carries no amendment, and neither fix
  story names RP-S018. The README was kept current (lines 535 to 543). *Instance:* fix now:
  append a dated amendment to RP-S018 saying #86 and #87 superseded decision 23. *Prevention:*
  a fix that changes a merged story's decision amends that story in the same PR.
- F4. Both build stories put their tests in new files rather than the shared files `epics.md`
  named, and both say why (RP-S017 and RP-S018 completion notes). *Instance:* accept.
  *Prevention:* none.

**Spec-to-implementation reconciliation.**

- F5. `label` refuses every false positive of a secret detector, by design, so FR-26 cannot serve
  the detector that needs field negatives most. AD-13 makes label refuse when redaction changes
  what the detector matched, and redaction always removes what a secret detector matched. The
  spike reports that all twenty `secrets/secret-in-write` false positives were refused, at a
  field precision of 0.05 (RP-SP002, "Field negatives"). This run reproduced it: a synthetic
  write that names an access-key variable in prose is a `secret-in-write` hit, and `label`
  refused it with "redaction changed what secrets/secret-in-write matched", exit 2.
  - FR-26 says "One command turns a hit the user says is wrong into a labelled negative" (PRD
    line 499) and does not state the refusal.
  - SM-8 counts negatives "that came from real false positives through FR-26" (PRD line 808);
    eight exist, all local, and none can come from a secret detector.
  - The README names the refusal generically ("redaction changed what the detector matched",
    lines 528 and 529) and does not say a secret detector's false positive is always refused, or that a
    hand-written synthetic stand-in is the way to record one. The refusal message offers no
    next step.

  *Instance:* fix now: state the limit in FR-26 and the README, and make the refusal say what to
  do instead. *Prevention:* when a spike's result shows a requirement cannot hold for a class of
  input, the spike story files the spec amendment.
- F6. The spike's method changed from the spec, and the spec still names the old one. `epics.md`
  story 4.3 and RP-SP002's title and Experiment say "hand-sampled" hits (`epics.md` lines 681 and
  693). The Result says every hit was judged by agents under the maintainer's delegation of
  2026-09-24, and the Decision records that the release text calls it "agent-judged". The
  delivery commit's subject says "sampled real hits". *Instance:* fix now, in the spec pass.
  *Prevention:* as F5.
- F7. Realised assumptions still carry their tags, for the fifth epic running (Epic 1 F4,
  Epic 2 F6, Epic 3 F4, Epic 5 F2): six in `epics.md`'s Epic 4 section (lines 634 to 699), four
  in RP-S017, three in RP-S018, one in RP-SP002, and two in AD-13 (`ARCHITECTURE-SPINE.md`
  lines 421 and 428), whose heading already says both commands are implemented. *Instance:* fix
  now, in the same spec pass as `epic-3-retro-item-18-...` and `epic-5-retro-item-27-...`.
  *Prevention:* `epic-2-retro-item-15-...`, still open.
- F8. The spike's two follow-ups are not tracked anywhere this run can see. RP-SP002's Decision
  names `secret-in-write`'s redesign and `whole-file-cat`'s redirected-group case as follow-ups
  after 0.2.0. `issue-map.json` has no item for either, and `deferred-work.md` has no entry after
  2026-09-23. They may exist on GitHub unmapped; this run did not look. *Instance:* fix now:
  file both with `scripts/bmad_issue_sync.py new`, or map them if they exist. *Prevention:*
  `epic-2-retro-item-14-...`, still open.

### Diff-scope review (`bmad-review`: adversarial, edge-case, verification-gap)

`bmad-review` resolved its three code lenses, and this run ran them one after another, inline,
because it had no subagents. They ran over the code in `ea82257` and `ecacae5` and over the
spike's Result in `fdfefe0`, weighting the boundaries: explain's address into label, label into
the corpus, the spike's use of both, and the four fixes' effect on label. Both build stories had
already been through a build review and a code review, and RP-S017 through two adversarial
redaction passes, so this pass did not re-hunt redaction or label's write and rollback paths.
No separate claims file was staged; the commit messages and the README were read as the claims.

- F5 (adversarial; reproduced) is above.
- F9 (verification gap). No test holds the AD-1 import graph. Searching `tests/` for an import
  graph or dependency-direction check finds none; `tests/test_contract.py` reads
  `detectors/common.py` by syntax tree for `SECRET_PATTERNS` only (line 539). So F1's two edges
  landed with the suite green. *Consumer:* the zip import the harness relies on, which AD-1
  protects. *Instance:* fix now, with F1. *Prevention:* this test.
- F10 (adversarial). The published precision leaves the `unsure` verdicts out of the
  denominator. `testing/test-after-change` shows 1.00 from 44 right, 0 wrong and 6 unsure of 50;
  counting the unsure as wrong gives 0.88. `cache-hygiene/model-switch` has one unsure, and
  `secret-in-write` two (RP-SP002, Result table). The table shows the Unsure column, so nothing
  is hidden in the story, but a release note that quotes the Precision column alone overstates
  two detectors. *Instance:* defer to story 6.2's release text. *Prevention:* none.
- F11 (adversarial). Several figures describe the reader before the spike's fixes. Round 1
  sampled `secret-in-write` from 40 hits (73 after the fixes) and `model-switch` from 98 (72
  after), and neither was sampled again. `no-verify` (0.17) and `unfiltered-find` (0.00) are
  labelled "before #88" and "before #98", and no after-fix figure exists; after #88, no-verify
  has one distinct right hit in the sample. The story labels each case, so this is a release-text
  risk, not a record defect. *Instance:* defer to 6.2's release text: carry the "before" labels
  and the sample populations. *Prevention:* none.
- Verification gap, otherwise: **No other verification gaps found.** Checked:
  - A known secret shape never reaches explain's item or text
    (`tests/test_explain.py` line 307 onwards) or label's written bytes (`tests/test_label.py`,
    the redaction refusal at line 296).
  - Explain writes and sends nothing (`tests/test_envelope.py`,
    `ExplainWritesAndSendsNothingTests`, line 354).
  - Explain's printed address is what label takes, for a subagent session
    (`tests/test_reader_subagents.py` line 147).
  - The emitter round trip and label's refusals and rollback (`tests/test_label.py`).
- Claims check. README line 42, "`label` writes only under the directory you name", held in the
  probe below. The spike's claim that `ruleprobe corpus --floor 0.9` passes with the new
  synthetic stand-ins held: 1.00 over 87 positives and 111 negatives.

### Process lessons (from story-file records)

- F12. The records lag the code again (Epic 3 F14, Epic 5 F11). Before this run,
  `sprint-status.yaml` showed 4.3 as `ready-for-dev` after `fdfefe0` merged, and
  `sprint_plan.py generate --dry-run` reported `in_sync: true` with it there. RP-SP002, RP-B005,
  RP-B007 and RP-B008 read `active` in `issue-map.json` and their frontmatter before this run's
  refresh. RP-S018, RP-B005, RP-B007 and RP-B008 say `Workflow status: review` and RP-B003 and
  RP-B004 `status: review`, all merged. RP-SP002 has no dev agent record and no Review findings
  section, the shape Epic 3 found in RP-SP001 (`epic-3-retro-item-21-...`). This run's refresh
  set RP-SP002, RP-B005, RP-B007 and RP-B008 `completed`, and epics RP-E005 (#24) and RP-E007
  (#26), which had closed on GitHub; the dev agent records are unchanged, and RP-E006 (#25) is
  still `active`. *Instance:* fix now: close out the records, and decide whether #25
  closes. *Prevention:* `epic-5-retro-item-30-...`, still open.
- What held, with sources:
  - The field-validity loop the epic was built for worked. The spike used `explain` and `label`
    on real transcripts and found four measurement bugs. Each became its own issue, story,
    regression test and, for #88 and #98, a synthetic stand-in in the shipped corpus
    (`560e60f`, `3028d3a`).
  - The one new matcher key, `git.config_regex` (#88), was put to the maintainer and recorded as
    an AD-7 decision in the spine memlog (line 113), rather than added silently. That is what
    `epic-3-retro-item-22-...` asks for, although the governance rule itself has not landed.
  - Every build decision in RP-S018 (26) and RP-S017 is recorded with its date, and every review
    finding with its resolution.
  - The spike published numbers only; its sample sheets and field corpus stay off the repository,
    as the governance rule on real transcript content requires.

## Behavior verification

This was run on `chore/retro-epic-4` at `fdfefe0`, which includes every Epic 4 commit, with this
run's sprint-status change uncommitted. Every file the probes wrote went to a scratch directory
outside the repository.

- **Gate.** compileall clean. `unittest` ran 1,160 tests, `OK`, none skipped.
  `ruleprobe corpus --no-config --floor 0.9` gave a total of 1.00 with floor 0.90 over 87
  positives and 111 negatives. `release_preflight.py` gave `0.1.0 is ready`. That is synthetic
  evidence, not field precision. The Python 3.9 run was not repeated here.
- **Explain (exit criteria 1 and 3).** `ruleprobe explain --root docs --rules docs/rules` printed
  three hits with session, key, detector, turn, tool use and command; the session hit printed
  that it names no tool use. `report` over the same input gave byte-identical output before and
  after, and `git status` showed no new file.
- **Label (exit criterion 2).** Labelling the `house-style/sudo-install` hit at `1:toolu_1` into
  an empty scratch corpus wrote one `.events.jsonl` and a new `labels.yaml` with one `near` label.
  `ruleprobe corpus --corpus <dir> --rules docs/rules` then scored it as one false positive. The
  session hit `2:-` was refused, and labelling the same name twice was refused, both with exit 2.
- **Probe (F5).** A synthetic Claude Code session held one `Write` whose content names an
  access-key variable in prose. `explain` printed it redacted; `label` refused it.
- **Not exercised here:** real transcripts, and the spike's figures, which rest on sample sheets
  kept on the measuring machine.

## Previous-retro follow-through

`sprint-status.yaml` holds 30 earlier action items. Epic 2's items 9 and 10 are `done`; the
other 28 are `open`. Five commits have landed since the Epic 5 retrospective's evidence point
(`6f0fd07`): `3028d3a` (#100), `a80f543` (#101, that retrospective), `644927a` (#103),
`560e60f` (#104) and `fdfefe0` (#105). `git diff --stat 6f0fd07 fdfefe0` over `ruleprobe/`,
`README.md`, `docs/`, `AGENTS.md`, `_bmad-output/planning-artifacts/`, `.github/`, `scripts/` and
`deferred-work.md` touches only `README.md`, the spine memlog, the corpus, `detectors/`,
`matchers.py`, `readers/gemini.py` and `shell.py`. So the evidence the Epic 5 retrospective
recorded for items 1 to 22 still holds unless noted.

- `epic-1-retro-item-1-run-the-series-contract-check-on-every-p`: not landed; no `v0.2.0` tag
  and no change under `.github/` or `scripts/`. Proposed status: none.
- `epic-1-retro-item-2-refuse-an-x-y-0-release-that-drops-a-dec`: not landed, same evidence.
  Proposed status: none.
- `epic-1-retro-item-3-decide-whether-the-series-contract-cover`: no evidence found. Proposed
  status: none.
- `epic-1-retro-item-4-answer-epics-md-open-questions-17-18-and`: not landed; `epics.md` is
  unchanged. Proposed status: none.
- `epic-1-retro-item-5-clear-the-stale-assumption-tag-in-the-sp`: not landed; the spine still
  holds 9 `ASSUMPTION` tags. Proposed status: none.
- `epic-1-retro-item-6-state-in-the-matchers-docstring-and-read`: no evidence found; the
  `matchers.py` change in `560e60f` adds `config_regex` only. Proposed status: none.
- `epic-1-retro-item-7-name-the-base-init-preflight-step-in-the`: not landed; `AGENTS.md` does not
  mention `--base-init`. Proposed status: none.
- `epic-1-retro-item-8-require-a-this-test-can-fail-line-red-be`: not landed;
  `docs/bmad-governance.md` has no such line. Proposed status: none.
- `epic-2-retro-item-11-make-the-compiled-detector-evaluation-me`: not landed;
  `memo[:] = [events, found]` stands at `ruleprobe/matchers.py` line 1088. Proposed status: none.
- `epic-2-retro-item-12-amend-ad-11-to-name-the-as-built-complia`: not landed; the spine names
  neither `compliance_rate` nor `min_opportunities`. Proposed status: none.
- `epic-2-retro-item-13-give-the-docs-worked-example-a-rule-that`: not landed; `docs/` is
  unchanged. Proposed status: none.
- `epic-2-retro-item-14-log-the-rp-s012-raising-callable-gate-qu`: not landed; `deferred-work.md`
  is unchanged. F8 is a new instance. Proposed status: none.
- `epic-2-retro-item-15-require-a-producer-to-consumer-test-for`: not landed;
  `docs/bmad-governance.md` is unchanged. F7 makes this the fifth epic running. Proposed
  status: none.
- `epic-3-retro-item-16-state-in-the-readme-that-one-catalog-sha`: no evidence found; the README
  changes since `6f0fd07` are the `config_regex` key, the Gemini note and the corpus table.
  Proposed status: none.
- `epic-3-retro-item-17-give-the-docs-worked-example-a-catalog-s`: not landed; `docs/` is
  unchanged. Proposed status: none.
- `epic-3-retro-item-18-clear-epic-3-realised-assumption-tags-in`: not landed; `epics.md` still
  holds 20 `ASSUMPTION` tags. Proposed status: none.
- `epic-3-retro-item-19-confirm-or-override-rp-s015-decisions-1`: not landed; `deferred-work.md`
  has no Epic 3 entry. Proposed status: none.
- `epic-3-retro-item-20-refresh-the-issue-map-so-rp-sp001-reads`: partly landed: RP-SP001 is
  `completed`, and this run's refresh set RP-E005 `completed` now that #24 is closed. RP-SP001's
  dev agent record still says `status: review` (line 138). Proposed status: `in-progress`.
- `epic-3-retro-item-21-add-rp-sp001-review-findings-and-replace`: not landed; RP-SP001 has no
  Review findings section. Proposed status: none.
- `epic-3-retro-item-22-require-in-docs-bmad-governance-md-that`: not landed;
  `docs/bmad-governance.md` has no such line. The practice was followed once: `560e60f` put
  `git.config_regex` to the maintainer and recorded it in the spine memlog (line 113), inside the
  fix rather than as its own item first. Proposed status: none.
- `epic-5-retro-item-23-read-a-user-edited-gemini-write-as-the-a`: landed. `644927a` (#103,
  `Closes #102`, RP-B008) keeps a user-edited `write_file` or `replace` native and drops the
  fields that may hold the user's text, with `tests/test_reader_gemini_user_edit.py`; that is
  the item's "or keep it native, with a regression test". Proposed status: `done`.
- `epic-5-retro-item-24-make-runtime-gemini-root-and-auto-root-r`: not landed;
  `ruleprobe/readers/__init__.py` is unchanged since `6f0fd07`. Proposed status: none.
- `epic-5-retro-item-25-decide-whether-a-cancelled-or-errored-ge`: not landed; neither RP-S019 nor
  RP-B008 mentions a cancelled call. Proposed status: none.
- `epic-5-retro-item-26-confirm-or-override-rp-s019-decision-11`: not landed; `deferred-work.md`
  is unchanged and RP-S019 is unchanged. Proposed status: none.
- `epic-5-retro-item-27-clear-the-realised-ad-3-assumption-tag-i`: not landed; the spine is
  unchanged but for one memlog line. Proposed status: none.
- `epic-5-retro-item-28-complete-the-readme-gemini-note-the-sand`: partly landed. `644927a` added
  the user-edited write to the README's Gemini gaps ("six gaps"). The sandbox root is still not
  named: the README has no `.cache/.gemini` or sandbox line. Proposed status: `in-progress`.
- `epic-5-retro-item-29-set-rp-s019-and-rp-t003-dev-agent-record`: partly landed. RP-S019 line 192
  and RP-T003 line 63 still say "implemented, awaiting review", but #26 has closed, and this
  run's refresh set RP-E007 `completed`. Proposed status: `in-progress`.
- `epic-5-retro-item-30-make-sprint-status-follow-the-issue-map`: not landed; this run had to set
  4.3 `done` by hand through `--set`, as the Epic 3 and Epic 5 runs did (F12). Proposed status:
  none.

This headless run proposes four transitions and passes no `--set-action-status`:
`epic-5-retro-item-23-...` to `done`, and `epic-3-retro-item-20-...`,
`epic-5-retro-item-28-...` and `epic-5-retro-item-29-...` to `in-progress`.

## Action items

Each item is proposed, not applied. Code and docs items go through the normal loop: an issue from
`scripts/bmad_issue_sync.py new`, then `bmad-build`. Spec items go through the named skill's
update intent. In `sprint-status.yaml` they are `epic-4-retro-item-31-...` to
`epic-4-retro-item-37-...`, in this order, each `open`.

1. **File the spike's two follow-ups** (F8): `secret-in-write`'s redesign and `whole-file-cat`'s
   `cat` inside a redirected `{ … }` group, with `scripts/bmad_issue_sync.py new`, or map them if
   they already exist on GitHub. Owner: maintainer. Process.
2. **Say what to do with a secret detector's false positive** (F5): the README's label paragraph
   says such a hit is always refused and that a hand-written synthetic stand-in is the way to
   record it, and the refusal message says the same. Owner: dev (bmad-build). Remediation.
3. **Reconcile AD-1 with `cli`'s two new imports and hold the graph with a test** (F1, F9): amend
   the AD-1 graph through `bmad-architecture` update, or route label's redaction and emitting
   through `report` and `validity`; then add a test that fails on an import edge the graph does
   not hold. Owner: architect, then dev. Spec reconciliation and remediation.
4. **Amend FR-26 and SM-8 for secret detectors, and reconcile the spike's method** (F5, F6):
   FR-26 states the redaction refusal; SM-8 says whether stand-ins written by hand count; and
   `epics.md` 4.3 and RP-SP002 say agent-judged, not hand-sampled. In the same spec pass, clear
   Epic 4's realised `[ASSUMPTION]` tags in `epics.md`, RP-S017, RP-S018, RP-SP002 and AD-13 (F7),
   beside `epic-3-retro-item-18-...` and `epic-5-retro-item-27-...`. Owner: pm and architect.
   Spec reconciliation.
5. **Carry the unsure counts and the "before" populations into 0.2.0's release text** (F10,
   F11), in story 6.2: each precision beside its unsure count, and the round-1 and before-fix
   figures marked as describing the reader before #86, #87, #88 and #98. Owner: dev (story 6.2).
   Remediation (docs).
6. **Amend RP-S018 decision 23** (F3) with a dated line saying #86 and #87 superseded it, and
   what label does now. Owner: dev. Spec reconciliation.
7. **Close out Epic 4's records** (F12): give RP-SP002 a dev agent record and a Review findings
   section, set the workflow status in RP-S018 and the four fix stories, and decide whether #25
   closes. Owner: maintainer. Process.

## Acceptance verdict

**accepted-with-open-items** (criteria: declared).

- **Declared criteria met.** `RP-E006.md` declares five exit criteria:
  1. `explain` prints session, turn, tool-use id, detector id and matched field for every hit,
     and a test proves a known secret shape never appears: observed here, and held by
     `tests/test_explain.py`. The "matched field" is the command or the tool's whole input,
     because a `Hit` records no field (RP-S017 completion notes); FR-25's "such as the command"
     covers it.
  2. `label` writes one `.events.jsonl` and one `near` label, and `corpus` counts it: observed
     here.
  3. `report` still writes and sends nothing, and no report number changes under explain: held
     by `tests/test_envelope.py` and observed here.
  4. `ruleprobe corpus --floor 0.9` passes: 1.00 on the synthetic corpus.
  5. RP-SP002 is run with its figures recorded before the 0.2.0 release notes: `fdfefe0`; story
     6.2 is still `ready-for-dev`.
- **Open items.** None fails a declared criterion.
  - F5 is the substantive one: FR-26's one command cannot record a secret detector's false
    positive, and SM-8 cannot be met from those detectors.
  - F1 and F9 are an undocumented import edge with no test to catch the next one.
  - F3, F6 and F7 are spec reconciliations; F7 recurs from every earlier epic.
  - F8 and F12 are record-keeping; F10 and F11 are deferred to 6.2's release text; F2 is
    deferred.
- `pending_stories` is empty after this run's sprint-status repair.
- **Status.** This is a machine verdict rendered in a headless run. No person has reviewed it, and
  a human decision overrides it.

## Open questions

- F5: should a secret detector's field negatives count toward SM-8 when written by hand from a
  real false positive, or does SM-8 need a different route for them?
- F2: should `label`'s corpus writing become a library function, as `explain` is, or stay a
  command only?
- F10: which figure should the 0.2.0 release text lead with for a detector with unsure verdicts:
  the precision over judged hits, or a range with the unsure counted both ways?
- Should #25 close? RP-E006 is `active` although all three children have merged.

## Assumptions

- The epic came from the invocation (`-H 4`), not from auto-detection.
- `detect-epic --epic 4` first returned `story_count: 3` and `pending_stories`
  `["4-3-spike-validity-field-precision-per-shipped-detector-from-han"]`.
- Following the orchestrator's instruction, the run set that key `done` with `sprint_plan.py
  generate --set`. The evidence was `fdfefe0`'s `Closes #53`. After the set,
  `detect-epic --epic 4` returned `pending_stories: []`, so the completeness gate raised nothing.
- The epic's commits were identified by the PR numbers in their subjects and matched to issues
  through each commit's `Closes` line and each story file. The four fixes #86, #87, #88 and #98
  were read as the spike's consequences, on the orchestrator's statement and the spike's Result.
  Eleven commits inside the envelope were excluded as other work.
- Evidence was local only: the git history, the story files and the planning corpus. The pull
  requests were not opened, GitHub was not searched for the spike's follow-ups (F8), and local
  orchestrator state was not read.
- `bmad-review` ran its three code lenses sequentially and inline, because no subagents were
  available, on a scope narrowed to the cross-story boundaries; it did not re-review redaction or
  label's write and rollback paths, which four earlier review rounds covered.
- F5 was reproduced with a synthetic session in a scratch directory outside the repository. The
  spike's figures were read as recorded; their sample sheets are not in the repository.
- Session logs were not available.
- The verdict **accepted-with-open-items** was rendered on the evidence alone, with no human
  decision.
- Action items 1 to 7 were proposed without confirmation. Their owners are roles, not people.
- No previous epic's action item was moved off `open`. The four proposed transitions are
  recorded above, and no `--set-action-status` was passed.
