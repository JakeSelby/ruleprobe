---
bmad_id: "RP-S013"
type: "story"
title: "feat(report): compliance per opportunity for order and absent detectors"
lifecycle: "active"
provenance: "authored"
github_issue: 43
github_issue_url: "https://github.com/JakeSelby/ruleprobe/issues/43"
parent_bmad_id: "RP-E004"
parent_github_issue: 23
updated: "2026-09-23"
---

# RP-S013 — feat(report): compliance per opportunity for order and absent detectors

<!-- bmad-sync:begin -->
- **GitHub issue:** [#43](https://github.com/JakeSelby/ruleprobe/issues/43)
- **Primary parent:** [RP-E004](https://github.com/JakeSelby/ruleprobe/issues/23)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As a developer with rules,
I want the report to show opportunities and followed beside hits per session,
so that I can say how often a rule was followed when it applied.

## Context and value

This is the user-facing half of maintainer decision 3. RP-S012 puts `compliance` in rows; this story reads it in `report_data` and `report`, applies the FR-22 minimum, settles the `promote?` wording (RP-D006) and folds renamed ids the same way hits fold. Today `report_data(rows, by, min_sessions, promote_share, registry, validity)` returns hits, sessions and share per detector with a `promote?` or `unobserved` note gated on `min_sessions`, and `report` prints the same numbers.

## Acceptance criteria

1. **Given** a fixture session with N opportunities of which M are followed, **when** `report_data` runs, **then** it gives `opportunities: N`, `followed: M` and `undecided`, and the table prints the same numbers beside hits per session, not in place of them.
2. **Given** a detector with no opportunity, **when** reported, **then** it prints hits per session only and no compliance figure.
3. **Given** fewer opportunities than the minimum (`min_opportunities`, a parameter of `report` and `report_data`, default from RP-D004), **when** reported, **then** counts print and no rate does; in `report_data` the rate field `compliance_rate` (`followed / opportunities`) is `null` and the table prints the same absence (FR-19). [ASSUMPTION: the field name `compliance_rate`, and `null` rather than an absent key]
4. **Given** `--by repo` or `--by stance`, **when** reported, **then** compliance is grouped the same way as hits.
5. **Given** the RP-D006 decision, **when** the table prints, **then** the threshold marker uses the decided wording and carries no advice.
6. **Given** the same rows, **when** reported twice, **then** `--json` is byte-identical.
7. **Given** rows whose `compliance` map is keyed by a retired detector id and rows keyed by its current id, **when** `report_data` runs, **then** `compliance` folds through the same fold function story 1.6 uses for `rules`, and `opportunities`, `followed` and `undecided` sum under the current id (AD-9, AD-11).

## Design

### Approach

Extend `report_data` to sum each row's `compliance` per group, after folding its keys through the one fold function (today `folded_rules`, generalised by 1.6). Add `min_opportunities` beside `min_sessions` to `report_data`, `report` and a CLI flag, and emit `compliance_rate` only at or above it. `report` prints the three counts and the rate in added columns beside the existing ones. Every map is sorted explicitly (NFR-3) so `--json` is stable. The `promote?` note changes only as RP-D006 decides.

### Decisions and alternatives

- The minimum's default and scope: RP-D004 (working default 20, per printed group, assumed).
- The marker wording: RP-D006.
- Compliance by position is out of 0.2.0 (RP-D007).
- 2026-09-23: `promote_share`, `--promote-share` and `RULE_PROMOTE_SHARE` are renamed to
  `frequent_share`, `--frequent-share` and `RULE_FREQUENT_SHARE`, as is the `report_data` key,
  following RP-D006 in the 0.2 break (the one release allowed to break the declared API). The
  spelling is a default the maintainer confirms or overrides.

### Do not implement

- No compliance by position in the session.
- No change to how hits, sessions or share are computed.
- No rate replacing hits per session.

## Tasks

- [x] Sum folded `compliance` per group in `report_data` (AC: 1, 4, 7)
- [x] Omit compliance for detectors with none (AC: 2)
- [x] Add `min_opportunities` to `report_data`, `report` and the CLI; `compliance_rate` null below it (AC: 3)
- [x] Print compliance columns in `report` matching `report_data` (AC: 1, 3)
- [x] Apply the RP-D006 wording (AC: 5), including the `frequent_share` rename (spelling
  pending the maintainer's confirmation)
- [x] Assert byte-identical `--json` (AC: 6)
- [x] Update the README report section

## Dev notes

### Bound decisions and requirements

- FR-21 Compliance per opportunity; FR-22 Minimum opportunities; FR-17 Hits per session (touched); FR-18 Grouping and window; FR-19 The report as data.
- AD-11 Opportunity and compliance travel beside hits, never inside them; AD-8 The package envelope; AD-9 The contract is versioned, once, in 0.2.0 (fold map).
- Depends on RP-D004, RP-D006, RP-S012 and story 1.6.

### Files to touch

- `ruleprobe/report.py`: `report_data`, `report`, `folded_rules` or the one fold function.
- `ruleprobe/cli.py`: the `--min-opportunities` flag. [ASSUMPTION: flag name mirrors `--min-sessions`]
- `README.md`: report section.

### Testing

- `tests/test_report.py`: AC1 to AC7, including a group under the minimum and a renamed id.
- `tests/test_cli.py`: the flag and `--json` stability.
- Run the `## Gate` block in `AGENTS.md`.

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 2.6]
- [Source: _bmad-output/planning-artifacts/prds/prd-ruleprobe-2026-09-23/prd.md#FR-21: Compliance per opportunity]
- [Source: _bmad-output/planning-artifacts/prds/prd-ruleprobe-2026-09-23/prd.md#FR-22: Minimum opportunities]
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-ruleprobe-2026-09-23/ARCHITECTURE-SPINE.md#AD-9: The contract is versioned, once, in 0.2.0]
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-ruleprobe-2026-09-23/ARCHITECTURE-SPINE.md#AD-11: Opportunity and compliance travel beside hits, never inside them]
- [Source: ruleprobe/report.py#report_data]

## Dev agent record

### Agent model used

Claude Opus 5.5 (`claude-opus-5-5`), as a builder in a task worktree.

### Completion notes

- `report_data` sums each measured row's `compliance` through `folded_compliance`, which folds ids
  with the same `_current` chain-follower `folded_rules` uses (AC7). A tally that is not three
  non-negative integers with `followed <= opportunities` is skipped, never read as zero.
- By rule, a detector some row carries compliance for (or whose `opportunities` failed) gets
  `opportunities`, `followed`, `undecided` and `compliance_rate`; any other detector gets none of
  the four keys (AC1, AC2). By repo and by stance, each group carries `compliance`, detector id to
  the same four figures, sorted by id, possibly empty (AC4).
- `min_opportunities` (default `RULE_MIN_OPPORTUNITIES = 20`, per printed line or group, per
  RP-D004) is a trailing keyword on `report_data` and `report` and the CLI flag
  `--min-opportunities`; below it, and at zero opportunities, `compliance_rate` is `None` and the
  table prints `-` (AC3). Undecided points do not count toward the minimum.
- The table adds `opportunities`, `followed`, `undecided` and `rate` columns only when some detector
  has compliance, so a report without any is byte-for-byte the old table; grouped tables add one
  indented line per detector with compliance.
- The marker is `frequent` (RP-D006, AC5), and the threshold follows it as `frequent_share`,
  `--frequent-share` and `RULE_FREQUENT_SHARE`; the declared call shape and its contract test
  moved with it. `RULE_MIN_OPPORTUNITIES` is exported from the package root.
- A stored tally that fails validation drops that session from the detector's compliance and is
  counted in `malformed_compliance` with a note. Opportunity-error and malformed counts are taken
  after the stance rescan filter; group compliance is seeded from them, and an id with only
  compliance or a failure gets a by-rule line. `min_opportunities` must be an int (TypeError).
- The validity column now starts past the widest note, so it lines up when a note is wider than
  its header (true before this story as well).
- Deferred from RP-S012: a session with an `opportunities`-hook error for a detector, or with a hit
  error for it, is out of that detector's compliance figures; `opportunity_errors` counts the
  failing sessions per detector and a note names them, so a failure is never silent.
- Verified with the `## Gate` block (all exit 0; 737 tests OK) and the 3.9 floor
  (`uv run --no-project --python 3.9`, 737 tests OK).

### File list

- `ruleprobe/report.py`
- `ruleprobe/cli.py`
- `README.md`
- `tests/test_compliance_report.py` (new)
- `tests/test_contract.py`
- `tests/test_report.py`
- `tests/test_cli.py`
- `tests/test_findings.py`
- `ruleprobe/__init__.py`
- `_bmad-output/implementation-artifacts/RP-S013.md`

## Review findings

None yet.

## Change log

- 2026-09-23: written from the planning corpus before implementation.
- 2026-09-23: implemented; compliance summed, folded and gated in the report, marker renamed to `frequent`, opportunity failures surfaced.
- 2026-09-23: review fixes; `frequent_share` rename, malformed tallies counted, group and id seeding, argument check, layout tests.
