---
bmad_id: "RP-C007"
type: "chore"
title: "docs(solutions): record two issue-map traps from the 0.2.0 build"
lifecycle: "active"
provenance: "authored"
github_issue: 122
github_issue_url: "https://github.com/JakeSelby/ruleprobe/issues/122"
parent_bmad_id: null
parent_github_issue: null
updated: "2026-09-24"
---

# RP-C007 — docs(solutions): record two issue-map traps from the 0.2.0 build

<!-- bmad-sync:begin -->
- **GitHub issue:** [#122](https://github.com/JakeSelby/ruleprobe/issues/122)
- **Primary parent:** None
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Goal

Record the two issue-map traps the 0.2.0 build hit, each with the command that recovers it, in
`docs/solutions/`, where the next session looks before it files or merges planning items.

## Acceptance criteria

1. **Given** `docs/solutions/2026-09-24-issue-map-traps.md`, **when** it is read, **then** it names
   the `new` command's failure to reserve and the `reserve` command that recovers it.
2. **Given** the same file, **when** it is read, **then** it says how to rebuild a merged issue map
   and to compare it by item, not by diff.
3. **Given** the gate, **when** it runs, **then** every step exits 0.

## Tasks

- [x] Write the note (AC: 1, 2)
- [x] Run the gate (AC: 3)

## Dev notes

Files: `docs/solutions/2026-09-24-issue-map-traps.md` (new), the issue map and this story.
No code change, and no user-visible change.

- [Source: docs/bmad-governance.md]

## Dev agent record

### Agent model used

Claude Opus 5.5 (`claude-opus-5-5`).

### Completion notes

- Filing this issue hit the first trap too: `new` filed #122 and failed to reserve it, and `reserve`
  recovered it.

## Change log

- 2026-09-24: written and done in one pass.
