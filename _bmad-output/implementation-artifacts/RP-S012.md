---
bmad_id: "RP-S012"
type: "story"
title: "feat(report): rows carry compliance for detectors that define opportunities"
lifecycle: "completed"
provenance: "authored"
github_issue: 42
github_issue_url: "https://github.com/JakeSelby/ruleprobe/issues/42"
parent_bmad_id: "RP-E004"
parent_github_issue: 23
updated: "2026-09-24"
---

# RP-S012 — feat(report): rows carry compliance for detectors that define opportunities

<!-- bmad-sync:begin -->
- **GitHub issue:** [#42](https://github.com/JakeSelby/ruleprobe/issues/42)
- **Primary parent:** [RP-E004](https://github.com/JakeSelby/ruleprobe/issues/23)
- **State:** completed

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Story

As a downstream tool storing rows,
I want each row to carry opportunity counts beside hit counts,
so that compliance can be reported from stored rows without re-reading transcripts.

## Context and value

Rows are the stored unit: a downstream tool keeps `measure()` rows and reports from them later. Today `measure()` calls `run()` and writes `rules` (detector id to hit count) and, on a raise, `rules_errors`. Without a `compliance` map in the row, RP-S013 cannot show compliance from stored data. AD-11 fixes the map's shape and says `measure()`, not `run()`, calls each enabled detector's `opportunities` with `run()`'s isolation.

## Acceptance criteria

1. **Given** a session and a detector with `opportunities`, **when** `measure()` runs, **then** the row carries `compliance[detector_id] = {"opportunities": N, "followed": M, "undecided": U}`, where `N` excludes `U` (AD-11): an undecided opportunity is counted in `U` only.
2. **Given** a detector with no `opportunities`, **when** `measure()` runs, **then** it has no `compliance` entry.
3. **Given** a detector gated off by the run's stances, **when** `measure()` runs, **then** its `opportunities` is not called.
4. **Given** an `opportunities` callable that raises, **when** `measure()` runs, **then** the raise is recorded in `rules_errors` against that detector and every other figure stands.
5. **Given** `run()`, **when** called as in 0.1.0, **then** its arguments and return are unchanged.

## Design

### Approach

In `measure()`, after `run()`, iterate the registry's detectors in a sorted order (NFR-3), skip any whose `enabled(stances)` is false or whose `opportunities` is `None`, call `opportunities(events, ctx)` inside the same isolation `run()` uses, and fold the triples into `{"opportunities", "followed", "undecided"}`: `True` adds to both `opportunities` and `followed`, `False` to `opportunities`, `None` to `undecided` only. Write `compliance` only when at least one detector defines opportunities. [ASSUMPTION: a row with no opportunity-defining detector omits the `compliance` key rather than writing an empty map] The row's `schema_version` comes from story 1.5.

### Decisions and alternatives

- Calling `opportunities` from `run()` was rejected: AD-11 keeps `run()` and its return unchanged.
- The hook's form follows RP-D005.
- 2026-09-23: `measure()` runs through a private `registry._run` that also returns the `Context` it built and the enabled detectors, so each session is analysed once and each compiled detector evaluated once; `opportunities` gets the same `ctx` and `ctx.events` as `fn`, as AD-11 asks. The public `run()` returns `_run`'s hits and is otherwise unchanged.
- 2026-09-23: an `opportunities` failure's `rules_errors` entry carries `"hook": "opportunities"`, an additive field. `errored_detectors` skips tagged entries because the detector's `fn` ran: its hits stand, and it loses only its `compliance` entry, so the report is unchanged.
- 2026-09-23: a malformed result is recorded as `MalformedOpportunities`, so it reads apart from a `TypeError` the callable raised. Malformed: not an iterable of triples, a `turn` that is not an integer, a `tool_use_id` neither a string nor `None`, a `followed` not exactly a bool or `None`, or a repeated `(turn, tool_use_id)` point.
- 2026-09-23: `compliance` covers enabled detectors only; the key is absent when none is enabled and defines `opportunities`, or when analysis failed.
- 2026-09-23: surfacing tagged `opportunities` failures in `report` and `report_data` belongs to story 2.6.
- 2026-09-23: a raising callable gate is not isolated. It propagates from `run()` exactly as it did in 0.1.0, so isolating it would change `run()`'s behaviour against AC 5; left open for the maintainer.

### Do not implement

- No change to `report`, `report_data` or the CLI (RP-S013).
- No folding of `compliance` by renamed id here; the fold happens on read (RP-S013, AD-9).

## Tasks

- [x] Call enabled detectors' `opportunities` in `measure()` and count triples (AC: 1, 3)
- [x] Omit detectors with no `opportunities` (AC: 2)
- [x] Record a raise in `rules_errors` against that detector only (AC: 4)
- [x] Leave `run()` untouched and assert its signature and return (AC: 5)

## Dev notes

### Bound decisions and requirements

- FR-21 Compliance per opportunity; FR-9 A failing detector costs only itself (touched); FR-28 Schema version on rows.
- AD-11 Opportunity and compliance travel beside hits, never inside them; AD-9 The contract is versioned, once, in 0.2.0.
- Depends on 1.5 (schema version on rows) and RP-S011.

### Files to touch

- `ruleprobe/report.py`: `measure`.

### Testing

- `tests/test_report.py`: a fixture session with known followed, not-followed and undecided opportunities (AC1); a detector without the attribute (AC2); a gated detector whose callable would fail the test if called (AC3); a raising callable (AC4); `run()` unchanged (AC5).
- Run the `## Gate` block in `AGENTS.md`.

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 2.5]
- [Source: _bmad-output/planning-artifacts/prds/prd-ruleprobe-2026-09-23/prd.md#FR-9: A failing detector costs only itself]
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-ruleprobe-2026-09-23/ARCHITECTURE-SPINE.md#AD-11: Opportunity and compliance travel beside hits, never inside them]
- [Source: ruleprobe/report.py#measure]

## Dev agent record

### Agent model used

Claude Opus 5.5 (claude-opus-5-5).

### Completion notes

- `measure()` runs the detectors through `registry._run`, a private variant of `run()` that also returns the `Context` it built and the detectors it found enabled. Each enabled detector defining `opportunities` is then called, in id order, with that same `ctx` and `ctx.events`, so a compiled detector's one-entry memo hits and `followed` comes from the evaluation that gave the hit. The first version analysed the session a second time, which built a new event list, missed the memo and evaluated every compiled `order` and turn-scoped `absent` detector twice. The public `run()` and its return are unchanged.
- A raise, or a result that is not an iterable of `(turn, tool_use_id, followed)` triples with `followed` exactly `True`, `False` or `None` (the check deferred from story 2.4), is appended to `rules_errors` with `"hook": "opportunities"`, and that detector gets no `compliance` entry. A malformed result is named `MalformedOpportunities`, so it reads apart from a `TypeError` the callable raised. `None`, a string, a dict or a non-iterable result is malformed; an empty list is a zero tally.
- `errored_detectors` skips entries tagged `"hook": "opportunities"`, so an `opportunities` failure costs no hit figure and the report is unchanged. The tag is an additive field on a `rules_errors` entry.
- The `compliance` key is written when at least one *enabled* detector defines `opportunities`, so a row whose only such detectors are gated off omits it; a row where every one raised carries `{}`.
- A detector whose `fn` raised keeps its `compliance` entry.
- `compliance` is not added to the declared public API: `measure` and row keys are not declared in the README or `tests/test_contract.py`. The `run()` test compares the signature against the call shape the README declares.
- Tests: `ComplianceTests` in `tests/test_report.py`. Mutation checks, each a `sed` edit of `ruleprobe/report.py` followed by `python3 -m unittest discover -s tests -p test_report.py`, then the file restored:
  - `if followed is None:` made never true (undecided counted as not followed): `FAILED (failures=12)`.
  - The `followed is %r, not True, False or None` raise replaced by `pass` (non-bool `followed` accepted): `FAILED (errors=3)`.
  - `_compliance` handed a fresh `analyse(session.events)` instead of the run's `ctx`: `FAILED (failures=2)`.
  - The `"hook": "opportunities"` skip in `errored_detectors` disabled: `FAILED (failures=1)`.
  - Restored: `OK`.

### File list

- `ruleprobe/registry.py`
- `ruleprobe/report.py`
- `tests/test_report.py`
- `_bmad-output/implementation-artifacts/RP-S012.md`

## Review findings

`bmad-build` review, three layers (blind hunter, edge-case hunter, verification gap):

- **Patched, high:** `measure()` analysed each session a second time, building a new event list, so the
  compiled detectors' one-entry memo never hit and each `order` and turn-scoped `absent` detector
  evaluated twice, against AD-11. A private `_run` now returns the context it built, and
  `opportunities` gets that same context and event list; a test counts one evaluation per detector.
- **Patched, medium:** an `opportunities` failure went into `rules_errors` in the same shape as an
  `fn` failure, so the report dropped the session from a working `fn`'s hit figures; the entry now
  carries `"hook": "opportunities"`, which hit figures ignore, and a malformed result is named
  `MalformedOpportunities`.
- **Patched, low:** the signature test reads the declared call shape; the mutation checks are recorded
  with their commands.

`bmad-code-review`, four layers (blind hunter, edge-case hunter, verification gap, acceptance
auditor):

- **Patched, medium:** the analysis-failure branch's return shape, which `run()` and `measure()` both
  depend on, had no test; one now forces `analyse` to raise.
- **Patched, low:** a triple's `turn` and `tool_use_id` are type-checked and a repeated point is
  refused; tests cover both hooks raising in one row, a disallowed gate variant and `strict=True`;
  the docstrings and README describe `compliance`, the `hook` tag and when the key is absent.
- **Not changed, for the maintainer:** a callable gate that raises stops `run()` and `measure()`, as it
  did in 0.1.0; isolating it would change `run()`, which AC 5 keeps as it is. Recorded under Design.
- **Deferred to story 2.6:** surfacing a tagged `opportunities` failure in the report.
- **Rejected:** "bump the schema version for `compliance` and `hook`": both ship in the release that
  introduces schema 2.
- **Patched, low:** the changelog carries the change, added when the branch is rebased onto `main`.

## Change log

- 2026-09-23: written from the planning corpus before implementation.
- 2026-09-23: implemented; `measure()` writes `compliance` and records a raising or malformed `opportunities` in `rules_errors`.
- 2026-09-23: review fixes; one analysis per session through a private `_run`, `opportunities` failures tagged `"hook": "opportunities"` and left out of hit figures, malformed results named `MalformedOpportunities`, the `run()` test read from the README.
- 2026-09-23: second review; `turn`, `tool_use_id` and repeated points validated, analysis-failure, strict, double-failure and disallowed-stance tests, `compliance` and the `hook` tag documented, design decisions dated.
