---
bmad_id: "RP-D004"
type: "decision"
title: "decision(report): the minimum opportunity count"
lifecycle: "active"
provenance: "authored"
github_issue: 38
github_issue_url: "https://github.com/JakeSelby/ruleprobe/issues/38"
parent_bmad_id: "RP-E004"
parent_github_issue: 23
updated: "2026-09-23"
---

# RP-D004 — decision(report): the minimum opportunity count

<!-- bmad-sync:begin -->
- **GitHub issue:** [#38](https://github.com/JakeSelby/ruleprobe/issues/38)
- **Primary parent:** [RP-E004](https://github.com/JakeSelby/ruleprobe/issues/23)
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Context

FR-22 says no compliance figure is shown below a minimum opportunity count, and that the minimum is a parameter of `report` and `report_data`. PRD Q7 asks whether 20 is the right value; the PRD's working default mirrors `min_sessions=20`, which today gates `unobserved` and `promote?` in `report_data`. AD-11 applies the minimum to `opportunities` and names 20 as the working default. The question: what is the default, and does it apply per rule or per printed group under `--by repo` and `--by stance`? A rate over three opportunities invites action on noise.

## Options

1. **20, per printed group.** Mirrors `min_sessions`, which the report already applies to each printed block. For: one mental model for both floors, and a `--by repo` block with few opportunities shows no rate. Against: a small repository may never show a rate.
2. **20, per rule across all groups.** For: a rule measured widely shows a rate in every group. Against: a group's rate can then rest on a handful of its own opportunities, which is what FR-22 prevents.
3. **A different default (lower or higher).** For: opportunities are per turn or per `first` match, so they may accrue faster than sessions and a higher floor could cost little. Against: no measurement in the corpus supports a specific other number yet.

## Decision

Open: the maintainer's answer to PRD Q7. The corpus's working default is 20 (FR-22, AD-11), and the epics file assumes per printed group. [ASSUMPTION: 20, per printed group]

## Consequences

- Option 1: RP-S013 applies `min_opportunities` wherever it applies `min_sessions`; the parameter default is 20; tests use a group under the floor.
- Option 2: RP-S013 computes the floor once per rule before grouping, and the table can show a rate in a group with fewer than 20 of its own opportunities.
- Option 3: the default changes in `report`, `report_data` and the CLI flag together, and the README states the value.
- Any option: the minimum stays a parameter, so a caller can override it.

## References

- [Source: _bmad-output/planning-artifacts/prds/prd-ruleprobe-2026-09-23/prd.md#FR-22: Minimum opportunities]
- [Source: _bmad-output/planning-artifacts/prds/prd-ruleprobe-2026-09-23/prd.md#11. Open questions] (Q7)
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-ruleprobe-2026-09-23/ARCHITECTURE-SPINE.md#AD-11: Opportunity and compliance travel beside hits, never inside them]
- [Source: _bmad-output/planning-artifacts/epics.md#Story 2.1]
- [Source: ruleprobe/report.py#report_data] (`min_sessions`)
