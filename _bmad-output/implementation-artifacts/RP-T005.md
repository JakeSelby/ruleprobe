---
bmad_id: "RP-T005"
type: "task"
title: "docs(planning): record the maintainer's 0.2 decisions"
lifecycle: "completed"
provenance: "authored"
github_issue: 62
github_issue_url: "https://github.com/JakeSelby/ruleprobe/issues/62"
parent_bmad_id: null
parent_github_issue: null
updated: "2026-09-23"
---

# RP-T005 — docs(planning): record the maintainer's 0.2 decisions

<!-- bmad-sync:begin -->
- **GitHub issue:** [#62](https://github.com/JakeSelby/ruleprobe/issues/62)
- **Primary parent:** None
- **State:** completed

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Goal

The maintainer's answers to all thirteen open 0.2 decisions, given on 2026-09-23, are recorded
where builders will read them: each decision's story file, the PRD, the architecture spine and
`epics.md`. After this change no 0.2 story waits on an unanswered question except the Gemini CLI
format check, which is an experiment rather than a decision.

## Acceptance criteria

1. **Given** each decision item RP-D001 to RP-D011, **when** its story file is read, **then** its
   Decision section states the maintainer's answer and date, and its Consequences follow the chosen
   option.
2. **Given** the PRD, **when** it is read, **then** each §11 open question the decisions settle is
   closed, FR-32 and FR-33 name Gemini CLI, FR-34 is withdrawn and moved to #21, and NFR-7 has no
   exception.
3. **Given** the spine, **when** `lint_spine.py` runs, **then** it reports no findings, and AD-3,
   AD-8, AD-9 and AD-12 match the decisions.
4. **Given** `epics.md`, **when** it is read, **then** every decision item shows its answer, Epic 7
   is marked withdrawn, and story 4.3 is no longer proposed.
5. **Given** the issue map, **when** `audit` and `audit --delivery` run for every changed item,
   **then** each reports 0 findings.

## Tasks

- [x] Record the thirteen answers in the decision and spike story files (AC: 1)
- [x] Amend the PRD through its update intent, logged in its memlog (AC: 2)
- [x] Amend the spine through its update intent, lint clean (AC: 3)
- [x] Mark `epics.md` and log it (AC: 4)
- [x] Put the field-precision spike (#53) on v0.2.0, and add drafting from prose to #21 (AC: 2, 4)
- [x] Refresh the issue map for the closed planning issues and audit it (AC: 5)
- [ ] After merge: close the decision issues and Epic 7's three issues with a link to this change
