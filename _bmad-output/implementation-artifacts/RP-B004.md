---
bmad_id: "RP-B004"
type: "bug"
title: "fix(readers): count a copied Claude Code session once"
lifecycle: "active"
provenance: "authored"
github_issue: 87
github_issue_url: "https://github.com/JakeSelby/ruleprobe/issues/87"
parent_bmad_id: null
parent_github_issue: null
updated: "2026-09-24"
---

# RP-B004 — fix(readers): count a copied Claude Code session once

<!-- bmad-sync:begin -->
- **GitHub issue:** [#87](https://github.com/JakeSelby/ruleprobe/issues/87)
- **Primary parent:** None
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Reproduction

From #87, at `0d8bc4b`, with Claude Code transcripts under `~/.claude/projects/`:

- A project directory that was copied or renamed, or a worktree with a project folder of its own,
  leaves one session in several files: `<folder>/<session id>.jsonl` under two or three project
  folders, and the same for each subagent's `<session>/subagents/agent-<id>.jsonl`.
- `iter_sessions` yields one session per file, so each copy's hits count again, and every copy
  enters a share's denominator. One detector's 12 reported hits were 6 distinct ones.
- On one developer's machine, re-measured at `b73baca` by key only, nothing copied: 5,630 files
  read as 5,524 sessions, of which 2,267 addresses appeared more than once, 2,307 sessions too
  many. By shape: 80 parent sessions as identical copies and 10 differing, and 2,104 subagent
  sessions as identical copies and 77 differing; every copy had its original's file name, in
  another project folder.

## Root cause

`iter_sessions` in `ruleprobe/readers/__init__.py` reads each path `_paths` returns and yields
whatever `read` gives, and FR-1 promised exactly that: one session per transcript file. Nothing
compares sessions across files. `session_address` already treats `<runtime>:<session id>` as
unique, and `label` has to refuse an address that names two sessions, so the rest of the package
assumed an id is one session while the readers did not deliver it.

## Acceptance criteria

1. **Given** two identical transcripts of one session in two project folders, **when**
   `iter_sessions` reads them, **then** it yields one session, from the first in path order, and
   its hits are counted once.
2. **Given** two differing files carrying one session id, **when** they are read, **then** the one
   with more events is the session; on equal event counts, the first in path order.
3. **Given** a parent transcript and its subagent's own transcript, **when** they are read,
   **then** they stay two sessions, `<id>` and `<id>/<agent id>`; a subagent file copied into two
   folders is one subagent session.
4. **Given** copies set aside, **when** `errors` is passed, **then** each is recorded with `error`
   `COPY` and `kept` naming the file read; **and** the CLI's read note counts them in a clause of
   their own, apart from `produced no session`, naming each with its folder.
5. **Given** a copy with no event, or one older than `since`, **when** the group is read, **then**
   it is noted `no session in it` or dropped silently, as alone, and is never the one kept.
6. **Given** the same files, **when** the directory walk lists them in another order, **then**
   `iter_sessions` yields the same sessions in the same order with the same notes, each session
   where its group's first file falls in path order.
7. **Given** Codex rollouts carrying one session id, **when** they are read, **then** the same
   rule applies, keyed by the rollout's own `session_meta` id.
8. **Given** a labelled corpus whose files share a session id, **when** it loads, **then** every
   file is its own session, and two files of one name in different folders are still refused.
9. **Given** the shipped corpus, **when** `ruleprobe corpus --no-config --floor 0.9` runs, **then**
   it scores 1.00, as before.
10. **Given** Gemini CLI sessions carrying one session id, nested subagent ids included, **when**
    they are read, **then** the same rule applies; **and** every reader has `session_key`, whose
    absence is an error rather than a silent read without grouping.
11. **Given** a `sessionId` that is a list or an object, **when** `iter_sessions` reads it, **then**
    the read does not abort and the id is its `str()`; **given** files that name no session id,
    **then** each is a session of its own, never grouped by its file stem.
12. **Given** a file whose id changed between the key pass and the read, **when** its group is
    read, **then** it is yielded under its own id, not set aside; **and** an address two groups
    reach is yielded once, the first yielded standing.
13. **Given** `report --json`, **when** copies were set aside, **then** `read_errors` holds failures
    only and `copies` lists the copies; **given** `label` refusing "no hit" where copies of the
    session were set aside, **then** the refusal names them with their folders.

## Design

Each reader gains `session_key(path)`, the id `read(path)` gives, computed by the same function
`read` uses: `_identity` and `_key` in the Claude Code reader, `_items` and `_key` in the Codex
reader. `iter_sessions` groups the paths by `(reader, key)` in a first pass, then reads each group
in order of its first path, keeps the session with the most events (the first on a tie), and
yields it. The rest are recorded in `errors` as `COPY`.

Design decisions:

1. **Pre-scan, not buffering.** A pre-scan of each file's key, then a read of each group, keeps
   `iter_sessions` streaming: two sessions are held at most. Buffering every session by id until
   the last file is read would hold the whole history at once, which `explain` is written to
   avoid. The cost, on the machine above (5,630 files): the key pass took 5.8 s, and a full
   `iter_sessions` 33.8 s against 26.3 s before, peak traced memory 524 MB against 503 MB. A
   parent file's key is decided by its opening lines; a subagent's own file must be read to the
   end, since its last line could still rule it out, and those are most of the files.
2. **One key function.** `session_key` and `read` share `_identity` (the first `sessionId`, and
   the agent id when every `user` and `assistant` line is one agent's sidechain) and `_key`
   (`<parent>/<agent>`, else the id, else the file stem). `_identity` stops once both are decided,
   so it serves a lazy line iterator and a list alike. A re-derived key would drift and merge a
   subagent into its parent.
3. **Most events, then path order.** A copy made mid-session is a prefix of the original, so the
   longer file is the fuller one. Path order is the tie-break because it is the order the walk is
   sorted into; nothing on disk, like a modification time, enters the choice (NFR-3).
4. **Candidates are files that would be sessions alone.** `since` and the no-event rule apply to
   each file first, so an old copy cannot hide a newer one, and an empty copy keeps its `no
   session in it` note: `COPY` means a session was read from another file, not a failure.
5. **Yielded where the group first appears.** The paths are sorted again before grouping, and the
   session comes out at its group's first path, so the order is a function of the paths alone,
   whatever order they were found in. The `COPY` notes come in path order too.
6. **When the passes disagree.** Only a file changing between the key pass and the read makes
   them disagree. A file read under another id than its group's is not that group's: it is
   yielded under its own id, after the group's session. `iter_sessions` also remembers each
   `(runtime, id)` it yielded, so an address two groups reach is yielded once. A stream cannot
   take a session back, so the first yielded stands and the later one is set aside as its copy,
   whatever its event count.
7. **Every runtime.** A Codex rollout's id is its own `session_meta`'s; a Gemini session's is the
   parent ids of the folders it is nested in, then its own `sessionId`, and `gemini.session_key`
   shares `_records`, `_meta_id` and `_key` with `read`. `session_key` is part of the reader
   contract (FR-4): `_group_key` does not catch a missing one.
8. **The corpus reads per file.** A label names its session by file name, and a near-miss is often
   a copied positive, edited, so `load_corpus` reads through `iter_file_sessions`, the same reader
   path without grouping. Not added to the README's API list.
9. **The note names copies apart.** `_read_errors_line` prints `N transcript(s) produced no
   session: ...` for failures and `M transcript(s) set aside as copies of a session read from
   another file: <folder>/<file> (kept <folder>/<file>)` for copies; a copy and the file kept
   usually share a name, so each carries its folder. Each clause names the first three and then
   says `, and N more`, as the failure clause always has: the note is one stderr line, and on a
   machine with thousands of copies a full list would bury the report. `report --json` carries
   every copy under `copies`, and `read_errors` holds failures only.
10. **A key is a session id.** `session_key` returns None for a file that names no session id and
    no agent id, and a non-string id is its `str()`, in the key and in `read` alike. A file with
    no key is a session of its own and is never compared, since two files known only by one stem
    are not known to be one session.
11. **`label` names the copies.** With one session per address, its ambiguity refusals could no
    longer fire and are removed. When the session read has no hit at the key and copies of it were
    set aside, the refusal names the file read and the copies, with folders, so `--root` can be
    pointed at the copy that holds the hit.

Rejected: buffering by id (memory, above); a cheaper key read from the file name, which a
subagent file's content and a stem-only file do not follow; choosing by modification time, which
is not deterministic across machines; merging the events of differing copies, which would invent
a session neither file holds.

The fix must not change: how a single file reads, the order of sessions with no copies, the
`no session in it` notes, the `since` rule, and the corpus scores.

## Regression test

`tests/test_reader_copies.py`, a new file: `ClaudeCodeCopyTests` (AC 1 to 3, 5, 6, 10 to 12),
`CodexCopyTests` (AC 7), `GeminiCopyTests` (AC 10), `NoteTests` (AC 4, 13) and `CorpusTests`
(AC 8); `tests/test_label.py` pins the label refusal (AC 13). In the first round: Against the readers at
`b73baca` it cannot import `COPY`; with that import shimmed it gives `Ran 15 tests`,
`FAILED (failures=7, errors=10)`. On this branch, `OK`. `tests/test_label.py`'s four copy tests
pinned the old behaviour (two sessions at one address) and now pin the new; against `b73baca`
the rewritten tests give `FAILED (failures=3)`. AC 9 is the corpus gate.
The walk-order test now reverses what `_paths` returns, and asserts that the reversal differs.

## Tasks

- [x] `session_key` in both readers, sharing the key function with `read` (AC: 3, 7)
- [x] `iter_sessions` groups by key, keeps the most events, first on a tie, notes copies (AC: 1, 2, 4)
- [x] `since` and the no-event rule decide candidacy first (AC: 5)
- [x] Order and notes independent of the walk (AC: 6)
- [x] `_read_errors_line` counts copies apart; `_labelled_event`'s docstring follows (AC: 4)
- [x] `load_corpus` reads per file through `iter_file_sessions` (AC: 8)
- [x] Regression tests in `tests/test_reader_copies.py`; `tests/test_label.py` updated (AC: 1 to 8)
- [x] README's `label` passage; PRD FR-1 amended (AC: 1)
- [x] Corpus floor (AC: 9)
- [x] `gemini.session_key`; `session_key` required (AC: 10)
- [x] Non-string ids, keyless files, a changed file and the address guard, each tested (AC: 11, 12)
- [x] `report --json` `copies`; `label` names the copies; ambiguity branches removed (AC: 13)
- [x] `COPY` and `iter_file_sessions` in `ruleprobe.readers.__all__`; README `iter_sessions`
  line and `corpus` exception; PRD FR-2 and FR-4 amended (AC: 4, 7, 8, 10)

## Dev notes

Bound: FR-1 Claude Code reader, amended; FR-2 Codex reader; FR-4 Reader contract (a reader gains
`session_key`); FR-26 `label` (an address is now one session); NFR-3 Deterministic output; NFR-4
Under-count rather than over-count; the no-event rule of RP-B003.

FR-1 was amended through `bmad-prd`'s update intent, headless: the consequence now reads one
session per runtime and session id, and a transcript with no events is none. After review, FR-4
(the contract gains `session_key`) and FR-2 (Codex rollouts group the same way) were amended by
the same route. The `.memlog.md` lines record both.

Files to touch: `ruleprobe/readers/__init__.py`, `ruleprobe/readers/claude_code.py`,
`ruleprobe/readers/codex.py`, `ruleprobe/readers/gemini.py`, `ruleprobe/cli.py`,
`ruleprobe/validity.py`, `README.md`, `tests/test_reader_copies.py`, `tests/test_label.py`, the
PRD and its `.memlog.md`, and this file.
`CHANGELOG.md` is added by the delivering session.

Testing: `tests/test_reader_copies.py`, `tests/test_reader_subagents.py`, `tests/test_label.py`,
`tests/test_validity.py`, and `ruleprobe corpus --no-config --floor 0.9`.

References:

- [Source: https://github.com/JakeSelby/ruleprobe/issues/87]
- [Source: _bmad-output/planning-artifacts/prds/prd-ruleprobe-2026-09-23/prd.md#FR-1: Claude Code reader]
- [Source: _bmad-output/planning-artifacts/prds/prd-ruleprobe-2026-09-23/prd.md#FR-4: Reader contract]
- [Source: _bmad-output/implementation-artifacts/RP-B003.md#Design]
- [Source: ruleprobe/readers/__init__.py#iter_sessions]
- [Source: ruleprobe/readers/claude_code.py#session_key]
- [Source: ruleprobe/readers/codex.py#session_key]
- [Source: ruleprobe/report.py#session_address]
- [Source: ruleprobe/validity.py#load_corpus]

## Dev agent record

- **status:** review
- **baseline_commit:** `b73baca`
- **Model:** Claude Opus 5.5, as the `builder` role.
- **Completion notes:** `session_key`, `_identity` and `_key` in the Claude Code reader, `_items`,
  `_key` and `session_key` in the Codex reader; `iter_sessions` groups, picks and notes through
  `_sessions` and `_pick`; `iter_file_sessions` for the corpus. The measurement in Reproduction
  and design decision 1 ran the base and the branch readers over local transcripts and printed
  counts and timings only.
- **Deviations:** `ruleprobe/validity.py` and `README.md` were outside the brief: the corpus
  loader would otherwise merge files a label names apart, and the README's `label` passage
  described two sessions at one address. `tests/test_label.py`'s four copy tests pinned the old
  behaviour and were rewritten. `_labelled_event`'s ambiguity branches were kept in the first
  round and removed in the review round.
- **Files:** `ruleprobe/readers/__init__.py`, `ruleprobe/readers/claude_code.py`,
  `ruleprobe/readers/codex.py`, `ruleprobe/readers/gemini.py`, `ruleprobe/cli.py`,
  `ruleprobe/validity.py`, `README.md`,
  `tests/test_reader_copies.py`, `tests/test_label.py`,
  `_bmad-output/planning-artifacts/prds/prd-ruleprobe-2026-09-23/prd.md`,
  `_bmad-output/planning-artifacts/prds/prd-ruleprobe-2026-09-23/.memlog.md`, this file.
- **Verification:** the repository's gate script
  (`.agent-harness/tools/gate.sh <worktree> 87`) on the staged review-round tree over `e7f509f`,
  each exit as it printed: `compileall: exit 0`; `unittest: exit 0` (`Ran 1090 tests`, `OK`);
  `unittest39: exit 0` (`Ran 1090 tests`, `OK`, Python 3.9.25); `corpus: exit 0` (`total … 1.00
  1.00 1.00 floor 0.90`); `audit: exit 0` (`57 issue(s), 0 finding(s)`); `audit_delivery: exit 0`
  (`depth: issue #87, 0 finding(s)`); `preflight: exit 0` (`release preflight: 0.1.0 is ready`).
  The first round, on the staged tree over `b73baca`, gave the same exits with `Ran 1022 tests`.

## Review findings

`bmad-code-review` of `e7f509f`, four layers (acceptance auditor, blind hunter, edge-case hunter,
verification gap). Every patch was verified on the full gate afterwards.

- **Patched, should-fix:** `gemini` had no `session_key`, and `_group_key` swallowed the
  `AttributeError`, so Gemini copies kept the first file, not the fullest. `gemini.session_key`
  added; a missing one now raises; `GeminiCopyTests` added.
- **Patched, should-fix:** a list or object `sessionId` made an unhashable key and aborted
  `iter_sessions`. Ids are `str()`ed in the key and in `read`; any key that is not a string groups
  by path. Tested.
- **Patched, should-fix:** files with no session id grouped by their stem and could merge. A file
  without an id has no key and is its own session. Tested.
- **Patched, should-fix:** a file whose id changed between the passes was set aside as a copy of a
  group it no longer belonged to. It is now yielded under its own id. Tested.
- **Patched, should-fix:** the cross-group address guard (decision 6) was untested and its
  first-stands rule undocumented. Tested and documented in `iter_sessions` and decision 6.
- **Patched, should-fix:** `COPY` and `iter_file_sessions` were not in `ruleprobe.readers.__all__`,
  and the README's `iter_sessions` line did not say `errors` holds copies. Both added; neither
  joins the README's declared list.
- **Patched, should-fix:** `report --json` mixed copies into `read_errors`. It now holds failures
  only, and `copies` lists the copies. Tested.
- **Patched, should-fix:** `label` refused "no hit" with no sign a copy held it, and no test pinned
  a hit only in the shorter copy. The refusal names the copies with folders; tested; the
  unreachable ambiguity branches are removed.
- **Patched, should-fix:** FR-4 and FR-2 were not amended. Both amended through `bmad-prd`, headless.
- **Patched, note:** the walk-order test patched `os.walk`, which `_walk` sorts away. Paths are now
  sorted before grouping and the test reverses `_paths` itself.
- **Patched, note:** `COPY` notes came in displacement order. They now come in path order. Tested.
- **Patched, note:** the README said copies are one session "in every command"; it now names
  `ruleprobe corpus` as reading each labelled file.
- **Patched, note:** the cap of three names then `, and N more` is recorded as decision 9 and
  tested.
- **Patched, note:** the gate's exit lines are recorded above.
- **Deferred to the delivering session:** the `CHANGELOG.md` line under `## Unreleased`.

## Change log

- 2026-09-24: written at filing.
- 2026-09-24: reproduction, root cause, acceptance criteria, design, tasks, dev notes and the dev
  agent record filled with the fix.
- 2026-09-24: after code review, `gemini.session_key` and a required reader contract, keyless and
  non-string ids, a file changed between passes, the address guard, path-ordered notes,
  `report --json` `copies`, `label` naming the copies, FR-2 and FR-4 amended (AC 10 to 13), and
  the review findings and gate exits recorded.
