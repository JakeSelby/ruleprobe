---
bmad_id: "RP-B003"
type: "bug"
title: "fix(readers): read a Claude Code subagent's own transcript as its own session"
lifecycle: "active"
provenance: "authored"
github_issue: 86
github_issue_url: "https://github.com/JakeSelby/ruleprobe/issues/86"
parent_bmad_id: null
parent_github_issue: null
updated: "2026-09-24"
---

# RP-B003 — fix(readers): read a Claude Code subagent's own transcript as its own session

<!-- bmad-sync:begin -->
- **GitHub issue:** [#86](https://github.com/JakeSelby/ruleprobe/issues/86)
- **Primary parent:** None
- **State:** active

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Reproduction

From #86, at `46f3528`, on Claude Code versions that write each subagent to
`<session>/subagents/agent-<id>.jsonl`:

- Every line of that file carries `isSidechain: true`, an `agentId` naming the subagent, and the
  parent's `sessionId`. Its first message line is a `user` line whose content is the subagent's
  prompt, as a string.
- `claude_code.read(path)` over such a file returns a `Session` whose `id` is the parent's session
  id and whose `events` is empty.
- On one developer's machine, during the field-precision spike (#53), 5,002 of 5,510 sessions read
  were such empty files. Every tool call a subagent made went unmeasured, and every share's
  denominator counted the empty files, so shares read about thirteen times too low.

## Root cause

`read` in `ruleprobe/readers/claude_code.py` sets `sidechain` from each line's `isSidechain` and
skips every sidechain `user` and `assistant` line and every sidechain compaction line. That is right
for an older parent file, where sidechain lines are another agent's work, and the module docstring
already says the subagent's own file "is read as its own session". Nothing in `read` tells the two
files apart, so the subagent's own file loses every line. `read` then returns a session whenever a
`sessionId` was seen, events or none, so each such file became an empty session under the parent's
id and entered every denominator.

## Acceptance criteria

1. **Given** a transcript in which every `user`, `assistant` and `system` line is `isSidechain` and
   names the same non-empty `agentId`, **when** `claude_code.read` reads it, **then** it is a session
   whose `id` is that agent id and whose events are its lines, wherever the file sits.
2. **Given** that subagent file, **when** it is read, **then** its first `user` line is the
   `user_prompt` of turn 1, tool results start no turn, each later prompt starts the next turn, the
   last assistant text of each turn is `final`, models and compactions are read, and the detectors
   fire on its tool calls.
3. **Given** a parent file holding its own lines and sidechain lines, with or without an `agentId`,
   **when** it is read, **then** the sidechain lines make no event, as before.
4. **Given** a file whose message lines name two agent ids, or whose sidechain lines name none,
   **when** it is read, **then** it is not treated as a subagent's own file.
5. **Given** a transcript of either runtime that yields no event, a Codex rollout with a session id
   included, **when** its reader reads it, **then** it returns None, and `iter_sessions` records it
   in `errors` as `no session in it`, in path order.
6. **Given** the README's `ruleprobe label` section, **when** it describes sessions, **then** it
   says a subagent's own transcript is its own session keyed by its agent id, and a transcript with
   no event is no session.
7. **Given** the shipped corpus, **when** `ruleprobe corpus --no-config --floor 0.9` runs, **then** it
   scores the same as before the fix.

## Design

The fix is in the two readers. A pre-pass in the Claude Code reader, `_own_agent(path)`, returns the agent id when
every `user`, `assistant` and `system` line of the file is `isSidechain` and all of them name one and
the same non-empty `agentId`, else `""`. When it returns an id, `read` treats sidechain lines as the
file's own and keys the session by that id; otherwise sidechain lines stay skipped. `read` returns
None for any transcript that yields no event.

Design decisions:

1. **Content, not path.** The file is recognised by its lines, not by `subagents/agent-*.jsonl`: a
   path layout can move or be copied into a corpus, while the line shape is what makes the lines
   this file's work. A parent file always holds its own non-sidechain lines, so it can never pass.
2. **Only message lines decide.** Other line types (`attachment`, `summary` and the like) carry no
   work and are ignored by the test, so a metadata line that lacks the markers cannot turn a
   subagent's file back into an empty one.
3. **One agent id, strictly.** Sidechain lines without an `agentId`, or naming two ids, are read as
   a parent's. Treating them as a subagent's own would guess, and NFR-4 prefers the quieter report.
4. **The id is the agent id, verbatim.** Not the parent's `sessionId`, which would give the parent
   and each of its subagents one address for `explain` and `label`; not the file name, which is
   path. No new `Session` field, since the public API is what the README lists.
5. **No event, no session, handled in the reader.** `read` returns None, the same signal as a file
   with no session in it, so `iter_sessions` needs no change: it records the path in `errors` with
   the existing note, the CLI prints it in its "produced no session" line, and `measure` never sees
   a row with nothing measurable. The note stays one string rather than a second reason, because
   `read` returns None for both and the distinction would need a change to the reader contract
   (FR-4). The Codex reader applies the same rule, since the decision is about what counts as a
   session, not about one runtime: a rollout with a `session_meta` and no event returns None too.
   The output stays deterministic: the pre-pass reads the file in line order and the notes
   come in path order.

Rejected: reading the parent's file and its `subagents/` directory together as one session, which
would merge two agents' turn numbering and final messages; and dropping empty sessions in
`iter_sessions`, which would hide them from a caller of `read` and differ per reader.

The fix must not change: how a parent file reads (turn numbering, `final` messages, compaction,
models, the de-duplication of a block written twice); how a Codex rollout with events reads; the
shipped corpus scores.

## Regression test

`tests/test_reader_subagents.py`, a new file, writes synthetic transcripts into a temporary
directory. `SubagentFileTests` (AC 1, 2), `ParentSidechainTests` (AC 3, 4), `NoEventTests` and
`CodexNoEventTests` (AC 5). Against the readers at `46f3528` it gives `Ran 14 tests`,
`FAILED (failures=10, errors=1)`; on this branch, `OK`. AC 7 is the corpus gate, run before and
after with identical output.

## Tasks

- [x] `_own_agent` pre-pass recognising a subagent's own file by its content (AC: 1, 4)
- [x] `read` keeps a subagent file's sidechain lines and keys the session by agent id (AC: 1, 2)
- [x] Parent files keep skipping sidechain lines (AC: 3)
- [x] `read` returns None for a transcript with no event (AC: 5)
- [x] Module and `read` docstrings state the rule (AC: 1, 5)
- [x] Regression tests in `tests/test_reader_subagents.py` (AC: 1 to 5)
- [x] The Codex reader returns None for a rollout with no event, with a test (AC: 5)
- [x] The README's `label` section states the new session rule (AC: 6)
- [x] Corpus floor, before and after (AC: 7)

## Dev notes

Bound: FR-1 Claude Code reader; FR-3 One event schema (`turn` increments on every `user_prompt`);
FR-4 Reader contract; NFR-3 Deterministic output; NFR-4 Under-count rather than over-count; AD-2 The
event schema is the one contract between readers and detectors.

FR-1's consequence "yields one session per transcript file" now reads as one session per transcript
file that yields an event; the file that yields none is counted in `errors`, as FR-1's third
consequence already requires of a file that cannot be read.

Files to touch: `ruleprobe/readers/claude_code.py`, `ruleprobe/readers/codex.py`, `README.md`,
`tests/test_reader_subagents.py`, this file and `_bmad-output/issue-map.json`. FR-1 is left as it
is; #87 amends it. `CHANGELOG.md` is added by the delivering session.

Testing: `tests/test_reader_subagents.py`, `tests/test_readers.py`, and
`ruleprobe corpus --no-config --floor 0.9`.

References:

- [Source: https://github.com/JakeSelby/ruleprobe/issues/86]
- [Source: _bmad-output/planning-artifacts/prds/prd-ruleprobe-2026-09-23/prd.md#FR-1: Claude Code reader]
- [Source: _bmad-output/planning-artifacts/prds/prd-ruleprobe-2026-09-23/prd.md#FR-4: Reader contract]
- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-ruleprobe-2026-09-23/ARCHITECTURE-SPINE.md#AD-2]
- [Source: ruleprobe/readers/claude_code.py#read]
- [Source: ruleprobe/readers/codex.py#read]
- [Source: ruleprobe/readers/__init__.py#iter_sessions]

## Dev agent record

- **status:** review
- **baseline_commit:** `46f3528`
- **Model:** Claude Opus 5.5, as the `builder` role.
- **Completion notes:** `_own_agent` and the empty-session rule in `ruleprobe/readers/claude_code.py`,
  with both docstrings updated. The line shape was checked against local Claude Code transcripts by
  key and type only, nothing copied: every subagent file's message lines were `isSidechain` with one
  `agentId`, its first message line a `user` prompt given as a string. Over those transcripts every
  parent session read identically before and after; every subagent file that read as an empty
  session now reads with events, its first event the turn 1 prompt, keyed by the id in its
  file name, including files nested under `subagents/workflows/<run>/`, which a path rule
  would have had to know about; files under `subagents/` that are not transcripts (workflow
  status lines) still give no session.
- **Codex:** added at the coordinator's request, applying the 2026-09-24 decision to every reader:
  `codex.read` returns None for a rollout with no event, whatever its `session_meta` says.
- **README:** the `ruleprobe label` passage that said a subagent's transcript shares its parent's
  session id now states the new rule; no other README passage or the `claude_code.py` docstring
  made the claim. The `_labelled_event` docstring in `ruleprobe/cli.py` still does, and is left
  for #87, whose copies are the remaining way two transcripts share an id.
- **Red before:** `tests/test_reader_subagents.py` against the readers at `46f3528`: `Ran 14 tests`,
  `FAILED (failures=10, errors=1)`.
- **Files:** `ruleprobe/readers/claude_code.py`, `ruleprobe/readers/codex.py`, `README.md`,
  `tests/test_reader_subagents.py`, `_bmad-output/issue-map.json`, this file.
- **Verification:** the `AGENTS.md` gate on the staged tree, each exit read directly; the corpus
  output is byte-identical to the baseline's.

## Review findings

None yet; this section is filled at code review.

## Change log

- 2026-09-24: written at filing; design, tasks and the dev agent record filled with the fix.
- 2026-09-24: widened to the Codex reader and the README's `label` section (AC 5, 6).
