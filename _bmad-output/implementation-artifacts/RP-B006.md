---
bmad_id: "RP-B006"
type: "bug"
title: "fix(report): an order detector opening on events with no tool-use id keeps its compliance"
lifecycle: "completed"
provenance: "authored"
github_issue: 91
github_issue_url: "https://github.com/JakeSelby/ruleprobe/issues/91"
parent_bmad_id: null
parent_github_issue: null
updated: "2026-09-24"
---

# RP-B006 — fix(report): an order detector opening on events with no tool-use id keeps its compliance

<!-- bmad-sync:begin -->
- **GitHub issue:** [#91](https://github.com/JakeSelby/ruleprobe/issues/91)
- **Primary parent:** None
- **State:** completed

The issue carries the summary, discussion and acceptance evidence; this file carries the design.

This work item was authored as part of the repository's committed BMad planning system.
<!-- bmad-sync:end -->

## Reproduction

At `8eb957d`, compile an `order` detector whose `first` opens on an event that is not a tool use,
and measure a session in which it opens twice in one turn:

- The detector: `{"id": "o/result-then-bash", "event": "session", "when": {"order": {"first":
  {"kind": "tool_result"}, "then": {"tool": "Bash"}}}}`.
- The session, all in turn 1: two `tool_result` events, then a `Bash` tool use. The results
  carry their own `tool_use_id`s; the opportunity points still name none (see Root cause).
- `measure()` gives `rules: {"o/result-then-bash": 2}`, `compliance: {}` and a `rules_errors`
  entry `{"error": "MalformedOpportunities", "hook": "opportunities"}`. The hits stand; the
  compliance is lost.

The same happens when `first` opens on two `assistant_text` or two `user_prompt` events in one
turn, and when it opens on two tool uses whose id is `""`, the id the Claude Code and Codex
readers give a tool use their transcript left without one. No shipped detector opens on an
event that gives an id-less point today. Found by the Epic 2 retrospective,
finding F8 (`epic-2-retro-2026-09-24.md`).

## Root cause

`_a_order` in `ruleprobe/matchers.py` builds each opportunity point with `_hit(event)`, which
names a `tool_use_id` only for a `tool_use` event and drops the one a `tool_result` carries, so
every other event it opens on gives `(turn, None)`; a tool use read with no id gives
`(turn, "")`. `_tally` in `ruleprobe/report.py` refused any repeated `(turn, tool_use_id)`
point as malformed, a rule RP-S012's second review added for hand-written callables. AD-11 says
each event `first` matches opens one opportunity, so the producer's output was right and the
consumer's rule was too wide: an id-less point names no event, so two of them are two events,
not one event counted twice. No test ran a compiled detector's output through `measure()`
(finding F9), so the conflict passed the suite.

## Acceptance criteria

1. **Given** an `order` with `first: {kind: tool_result}` and two tool results in turn 1 followed
   by a `Bash` call, **when** `measure()` runs, **then** the row's compliance is `{"opportunities":
   2, "followed": 2, "undecided": 0}` and it has no `rules_errors`.
2. **Given** a hand-written `opportunities` that repeats `(turn, None)`, **when** `measure()` runs,
   **then** each triple counts, decided or undecided.
5. **Given** an `order` that opens on two tool uses in one turn whose id is `""`, **when**
   `measure()` runs, **then** it keeps its compliance, as for `None`.
3. **Given** a hand-written `opportunities` that repeats a `(turn, tool_use_id)` point with an id,
   **when** `measure()` runs, **then** it is still refused as `MalformedOpportunities`.
4. **Given** every compiled detector in the AD-11 identity test, the shipped catalog's included,
   **when** its result over each session passes through `measure()`, **then** `measure()` accepts
   it and its compliance equals the triples' own counts; a detector `measure()` refuses fails the
   test.

## Design

### Approach

`_tally` refuses a repeated point only when its `tool_use_id` is non-empty (`if tool_use_id:`); an
id-less point, `None` or `""`, is never recorded in the seen set, so each such triple counts. `_tally` is the one place triples are
validated: `_compliance` calls it for `measure()`, and the report's `_is_tally` checks summed
tallies, not triples. The rule is explained in `_tally`'s docstring, and stated in the public contract: the
`Detector` docstring's `opportunities` entry and the README's `compliance` paragraph.

### Decisions and alternatives

- 2026-09-24: a repeated point with no tool use id, `None` or `""`, is several opportunities,
  not a malformed result; a repeated point with an id stays refused, since an id names one event
  and two triples for it count that event twice. This narrows RP-S012's 2026-09-23
  malformed-result decision; RP-S012 carries an appended amendment pointing here.
- 2026-09-24, trade-off: an id-less repeat is counted from any producer, a turn-scoped `absent`
  and a hand-written callable included, so a buggy callable that repeats `(turn, None)`
  over-counts with no error, against the package's under-count rule. Accepted because two
  id-less points cannot be told apart: refusing them refuses a correct `order`.
- 2026-09-24: `""` counts as id-less. The Claude Code and Codex readers default a missing tool use
  id to `""`, so a compiled `order` opening on two such tool uses gives `(turn, "")` twice.
- Rejected: giving id-less points an identity in `_a_order`, such as the event's index. It would
  change the triple shape AD-11 and the public `Detector` docstring fix, and a hand-written
  callable would still hit the same refusal.
- Rejected: dropping the repeated-point check altogether. The id-bearing case is a real detector
  error, and RP-S012's test for it stays.

### Must not change

The triple shape, `run()`, hit counts, the other malformed-result checks, and the
`MalformedOpportunities` name a row records.

## Regression test

- `tests/test_opportunity_points.py::test_an_order_opening_on_two_results_in_one_turn_keeps_its_compliance`
  is the retrospective's reproduction through `measure()`. Against `8eb957d`'s `report.py` it fails
  with `compliance: {}`; with the fix it passes. Its siblings cover `assistant_text` and
  `user_prompt` openings, two tool uses with `""` ids, an id-less hand-written repeat, and the
  id-bearing refusal.
- `tests/test_opportunities.py`'s identity test now adds the shipped catalog, an `order` opening on
  `tool_result` and a session with two results in one turn, and passes every result through
  `measure()`. Against `8eb957d`'s `report.py` it fails for `o/result-then-bash`.

## Tasks

- [x] `_tally` refuses a repeated point only when it carries a tool use id, with the rule in its
  docstring (AC: 1, 2, 3)
- [x] Drop the id-less case from `test_a_malformed_result_is_the_detectors_error` (AC: 2)
- [x] New `tests/test_opportunity_points.py`: the reproduction, other id-less openings, and the
  hand-written cases both ways (AC: 1, 2, 3)
- [x] The identity test takes the catalog and an id-less `order`, and runs each result through
  `measure()` (AC: 4)
- [x] Review: `""` treated as id-less, with a test (AC: 5)
- [x] Review: the trade-off in `_tally` and here; the repeat rule in the `Detector` docstring and
  the README; RP-S012 amended; retrospective items 9 and 10 marked done

## Dev notes

### Bound decisions and requirements

- AD-11: each event `first` matches opens one opportunity; the compiler derives `followed` from
  the evaluation that gives the hit; a test asserts the identity for every compiled detector.
- RP-S012's malformed-result decision, narrowed here as recorded under Design.
- `AGENTS.md` "The package contract": under-count rather than over-count; the change counts no
  opportunity that was not already a triple the detector returned.

### Files to touch

`ruleprobe/report.py`, `ruleprobe/registry.py`, `README.md`, `tests/test_report.py`,
`tests/test_opportunities.py`, a new `tests/test_opportunity_points.py`,
`_bmad-output/issue-map.json`, `_bmad-output/implementation-artifacts/RP-S012.md`,
`_bmad-output/implementation-artifacts/sprint-status.yaml` and this file. `issue-map.json`'s
`next_ids.bug` moves from 3 to 7, not 4: #86, #87 and #88 reserved RP-B003 to RP-B005 in parallel
on their own branches. A user-visible
fix, so `CHANGELOG.md` gains a line under `## Unreleased`, added by the orchestrator.

### Testing

`python3 -m unittest discover -s tests`, then the full gate in `AGENTS.md`.

### References

- [Source: _bmad-output/planning-artifacts/architecture-spines/architecture-ruleprobe-2026-09-23/ARCHITECTURE-SPINE.md#AD-11: Opportunity and compliance travel beside hits, never inside them]
- [Source: _bmad-output/implementation-artifacts/RP-S012.md#Decisions and alternatives]
- [Source: _bmad-output/implementation-artifacts/epic-2-retro-2026-09-24.md#F8, F9]
- [Source: ruleprobe/matchers.py#_a_order]
- [Source: ruleprobe/report.py#_tally]

## Dev agent record

- **Workflow status:** review
- **Baseline commit:** `8eb957d`

### Agent model used

Claude Opus 5.5 (`claude-opus-5-5`), as a builder in a task worktree.

### Completion notes

- Red before, round 1: with `8eb957d`'s `ruleprobe/report.py` restored, four tests in
  `tests/test_opportunity_points.py` and the identity test's `o/result-then-bash` subtest
  (session 81, the `NO_ID` session) fail; with the fix all pass.
- Red before, round 2: against `8eb957d`'s `report.py`,
  `python3 -m unittest discover -s tests -p "test_opportunit*.py"` gives `Ran 8 tests`,
  `FAILED (failures=6)`. Against round 1's check (`if tool_use_id is not None:`) it gives
  `FAILED (failures=1)`, the `""` test. With `if tool_use_id:` it gives `OK`.
- The identity test's detector set grows by the catalog's 7 entries and one `order`; the
  catalog's `testing/test-after-change` is now in it. Its one length assertion,
  `len(catalog.ENTRIES) == len(_CATALOG)`, counts catalog entries, not opportunities, and a
  detector with no triples is compared with an all-zero tally, which `_tally` returns, so a
  detector with zero opportunities cannot fail it.
- Gate, round 1, on the staged tree (`.agent-harness/tools/gate.sh`): `compileall`, `unittest`,
  `unittest39`, `corpus`, `audit`, `audit_delivery` and `preflight` all `exit 0`; `Ran 991 tests`
  `OK` on the default Python and on 3.9.25; `audit: 55 issue(s), 0 finding(s)`; `depth: issue
  #91, 0 finding(s)`; `release preflight: 0.1.0 is ready`.
- Gate, round 2, on the staged tree: the same seven checks `exit 0`; `Ran 992 tests` `OK` on the
  default Python and on 3.9.25; the corpus total at `1.00`, `floor 0.90`; `audit: 55 issue(s), 0
  finding(s)`; `depth: issue #91, 0 finding(s)`; `release preflight: 0.1.0 is ready`.

### File list

- `ruleprobe/report.py`
- `tests/test_report.py`
- `tests/test_opportunities.py`
- `tests/test_opportunity_points.py`
- `ruleprobe/registry.py`
- `README.md`
- `_bmad-output/issue-map.json`
- `_bmad-output/implementation-artifacts/RP-S012.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `_bmad-output/implementation-artifacts/RP-B006.md`

## Review findings

Code review, four layers (blind hunter, edge-case hunter, verification gap, acceptance auditor):

- **Patched, high:** an empty-string id was still refused when repeated, and the readers give a
  tool use with no id `""`. `_tally` now checks `if tool_use_id:`, with a test (AC 5).
- **Patched, should-fix:** the over-count trade-off of counting every id-less repeat was
  unstated. It is in `_tally`'s docstring and under Design.
- **Patched, note:** the repeat rule was private to `_tally`. It is in the `Detector` docstring
  and the README.
- **Patched, note:** the test comment and this file said tool results carry no tool use id.
  They carry one; `_hit` drops it for anything that is not a tool use. Reworded.
- **Patched, note:** `tests/test_opportunities.py`'s docstring now lists every session set,
  `UNDECIDED` and `NO_ID` included.
- **Patched, note:** RP-S012's dated decision still read as the old rule. An amendment line is
  appended; the dated entry is unchanged.
- **Patched, note:** retrospective items 9 and 10 stayed `open`. Marked `done` with the
  retrospective skill's `sprint_status.py update --set-action-status`.
- **Patched, note:** the `next_ids.bug` jump is explained under Dev notes.
- **Patched, note:** the red-before runs and gate output are recorded under Dev agent record.
- **Not changed:** the length assertion in the identity test counts catalog entries and cannot
  fail on a detector with zero opportunities (see Completion notes).
- **Not changed:** `CHANGELOG.md` gets its `## Unreleased` line from the orchestrator in the same
  pull request.
- **Accepted:** the extra tests (the same id in two turns, an id-bearing repeat beside id-less
  ones) sit at the boundary of AC 3 and stay.

## Change log

- 2026-09-24: written; the fix, its tests and the design decision recorded.
- 2026-09-24: code review; `""` treated as id-less, the trade-off and public contract stated,
  RP-S012 amended, retrospective items 9 and 10 done, runs and gate output recorded.
