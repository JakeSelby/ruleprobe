---
scope: ruleprobe package: readers, event schema, shell parse, registry, matchers, declarative format, rule binding, report, validity, CLI; 0.1.0 as built plus the v0.2.0 PRD
purpose: build-substrate
altitude: feature
updated: 2026-09-23T20:55
---

- (event) Create intent, headless Fast path, feature altitude, build-substrate, per issue #14 (RP-S005); run folder set by the brief, not customize.toml spine_output_path
- (event) Inputs: PRD 2026-09-23 (updated after validation, 18 findings applied) with addendum, memlog, validation-report.md; ruleprobe code at ae84ac3 (every module docstring, labels.yaml and common.yaml headers, tests/test_equivalence.py); pyproject.toml; .github/workflows; agent-harness spine read from GitHub main at 6869009 (AD-13, AD-21)
- (constraint) Inherited read-only from agent-harness spine: AD-13 (detector logic lives in ruleprobe; harness vendors a pinned wheel, never forks; a release means a harness bump; declarative detectors are read through the same engine) and AD-21 (stdlib first on a Python 3.9 floor; third-party code enters the harness only as a pinned wheel loaded by path)
- (constraint) Nine maintainer decisions of 2026-09-23 inherited as settled; FR, NFR and SM ids used as the PRD numbers them
- (decision) Paradigm confirmed with a correction: pipes-and-filters, but batch per session, not a streaming event pipe. iter_sessions streams sessions; within a session the event list is materialised, analysed once, and handed whole to every detector (session matchers need the whole list). Prevents a builder adding a per-event streaming filter that session detectors cannot use
- (decision) Dependency direction ratified from the imports: events and declarative are leaves; shell over events; registry over events and shell; matchers over registry, declarative, shell, events; rules over matchers, registry, declarative; readers over events only; report over registry; validity over readers, registry, declarative, events; cli over all. Three sanctioned function-local upward imports: registry.from_spec to matchers, report._validity_note to validity, and the tail import of detectors.common by registry to populate DEFAULT
- (decision) Event schema in events.py is the single contract; readers emit only its five kinds and documented fields; detectors read through input_of, text_of and .get; no detector branches on runtime
- (decision) One tool vocabulary: Claude Code's tool names and input keys are canonical; a reader translates at read time in a module-level table and keeps the native key beside the canonical one (codex cmd plus command); a tool with no canonical twin keeps its native name and so under-counts
- (decision) Under-count invariant for a new matcher: undecidable input yields no hit. Code check found a hole: command returns False on an unparsed command, so not and absent over it fire. Rule set for 0.2: a three-valued match (true, false, unknown); not of unknown is unknown; absent with any unknown candidate in scope yields no hit. Fixing the existing matchers changes counts, so it is logged as an open question for the 0.2 break
- (decision) Shared shell parse is the only Bash decomposition: run calls analyse once; detectors read ctx.bash; no detector or matcher tokenises a command itself; regex over the raw command text is a text read, not a decomposition
- (decision) Corpus contract ratified: labels keyed exactly as a Hit (turn:tool_use_id or turn:-); labels are the whole truth for each detector they name; sessions are synthetic native-runtime transcripts read through the real readers; floor 0.9 in CI is never lowered; unscored is allowed and visible; every shipped detector (catalog included) is scored
- (decision) Declarative format stays a closed YAML subset in declarative.py with JSON as an equal spelling; no YAML dependency; extending the subset or the matcher vocabulary is a contract change; a Python shipped detector with a declarative twin stays under test_equivalence
- (decision) Versioned contract keys: schema_version integer, absent means 1; on each detector entry (a file-level version key sets the default for its entries) and on each row and report_data result; a version above the package's known maximum is a finding for entries and a refused row for reports. Value 0.2.0 writes is PRD Q9
- (decision) Fold map: one resolution function over the union of a committed shipped map in the package and the consumer's Registry(renamed=) map, consumer entries winning; chains resolve transitively; a cycle is an error at registry build; report_data emits the effective map beside its rows so stored JSON folds without the registry
- (decision) Declared public API has one source: a contract test module lists every declared name and call shape (PRD FR-30), and the README renders from it; a name joins only with its test; SECRET_PATTERNS stays a plain ast.Assign literal list; no import-time file read so the wheel imports from a zip on sys.path
- (decision) Envelope: stdlib only; no module imports socket, urllib, http or a model client; open() only for reading except the FR-26 label command writing under a user-named directory; the clock only through --since; every output order comes from an explicit sort, never from dict, set or filesystem order
- (decision) New reader: a module under readers/ with ROOT, transcripts(root) sorted, read(path) returning Session; one RUNTIMES entry; imports events only; owns its tool-name table; derives turn and final like the others; skips a malformed line and records an unreadable file in errors; lands with a labelled corpus session and a README note of what it does not record
- (decision) Opportunity channel for 0.2: Detector gains an optional keyword attribute opportunities, a callable over (events, ctx) returning (turn, tool_use_id, followed) triples; fn(events, ctx) and the positional five-argument constructor are unchanged; order and absent compile it; rows carry compliance {detector_id: {opportunities, followed}}; report applies the minimum. Q10 stays open for the Python ergonomics
- (decision) Rule binding for 0.2: rules.py owns binding and the rule id; a section rule id is the rule file path relative to --rules plus a slug of the heading text; a catalog entry carries one anchored pattern and binds on exactly one match, else unmeasured; binding reads text with no model; a binding records its source (own or catalog)
- (decision) Explain and label: explain reruns detectors over transcripts and prints from Hit keys, never widening the row; one redact function owned beside SECRET_PATTERNS is the only path for text printed by explain or written by label; label writes one session and one near label under a directory the user names
- (assumption) Canonical rule id form (relative path plus heading slug) and the explain and label subcommand names are inferred; PRD left the CLI form to the spine
- (assumption) schema_version as the key name, and a file-level version key as the entry default, reconciling common.yaml's existing top-level version: 1 with FR-27's per-entry version
- (assumption) Fold persistence as a committed shipped map plus the map emitted in report_data, rather than a map written into every row
- (assumption) Opportunity as a second optional callable on Detector rather than a second return value from fn, to keep the harness's fn(events, ctx) shape
- (assumption) Three-valued matcher result as the mechanism for under-counting negation
- (version) Stack verified against repository files, not the web: requires-python >=3.9 and setuptools>=61 in pyproject.toml; CI tests 3.9 and 3.x; build==1.6.1; actions/checkout v7.0.1, actions/setup-python v7.0.0, actions/upload-artifact v7.0.1, actions/download-artifact v8.0.1, pypa/gh-action-pypi-publish v1.14.2, each SHA-pinned in .github/workflows
- (question) No CI job imports the wheel from a zip on sys.path, which is how the harness loads it; the contract test should add one
- (event) Distilled ARCHITECTURE-SPINE.md from this memlog and the code sweep; input reconciliation against the PRD, addendum and validation report done in-context; every FR-1 to FR-34 and NFR-1 to NFR-9 mapped
- (event) Reviewer gate: lint_spine.py 0 findings. The rubric walker, verification and adversarial lenses ran in-context, sequentially, each against the unedited draft, not as subagents (the worker cannot spawn them); full text in reviews/. The maintainer's session runs an independent validation after this
- (change) Applied from review: AD-3 Edit keys cut to what common.py reads; AD-6 binds explain and label to hit_key and event_key and adds the label-written .events.jsonl corpus format; AD-11 keeps run() unchanged and makes measure() own the opportunities call with the same gate and isolation; AD-12 fixes the file-level rule id and detector precedence (shipped, catalog, discovery order); AD-13 label writes .events.jsonl; new AD-14 ratifies that DEFAULT is never mutated after import
- (assumption) Label-written corpus sessions use an event-schema .events.jsonl format because a native transcript cannot be rebuilt from one event
- (question) Fix not and absent over unparsed input (AD-4) in the 0.2 break? It changes counts for any detector using them
- (question) Confirm schema_version as the key and a file-level version default (AD-9), with PRD Q9
- (question) Confirm fold persistence as a shipped map plus the map emitted in report_data, not a map in every row (AD-9, FR-29)
- (question) Confirm the opportunity channel as a keyword-only Detector attribute called by measure() (AD-11, PRD Q10)
- (question) Confirm the explain and label subcommand names, the .events.jsonl label format, and the rule id form path#heading-slug (AD-12, AD-13)
- (question) Action SHA-to-tag mapping in .github/workflows was read from pin comments, not verified
- (event) Status left draft per the caller; not finalized
- (event) Update intent, headless, per issue #14 (RP-S005). Change signal: validation-report.md in this folder, 14 findings (4 high, 5 medium, 5 low), run in a fresh context at HEAD 91ce821. AD ids kept stable; validation-report.md and reviews/ not edited
- (event) Reviewer gate: the fresh-context validation that produced validation-report.md is the independent review the gate requires. The author's in-context lens passes in reviews/ are kept as working notes only and do not count as the gate
- (direction) Maintainer direction via the caller: the AD-4 over-count is reproduced and filed as issue #19 (RP-B001); it reaches git and env as well as command; the fix lands under #19 in v0.2.0
- (decision) High 1 and medium 1, AD-4 rewritten: a segment matcher (command keys other than regex and unparsed, every git key, every env key) over a command the parse skipped returns undecided; any, all and not follow three-valued logic; not, absent and order treat undecided as no hit; a detector whose when is undecided produces no hit. Cites #19. The change alters counts for user detectors using not or absent, so it is named as part of the 0.2 break in AD-9. Prevents a negated or absence matcher counting an unreadable command as proof that something did not happen
- (decision) High 1, AD-11 polarity from the code, not the glossary: absent has no trigger key and its of is the action the rule asks for (matchers.py docstring). For absent scope turn, the opportunity is each turn present in the event list and followed means of matched in that turn; a hit is an opportunity not followed. For order, the opportunity is each event first matches and followed means then matched within the window; a hit is a followed opportunity. The compiler derives followed from the same evaluation that yields the hit, and a test asserts hits = opportunities - followed for absent and hits = followed for order on every compiled detector. Prevents a matchers unit and a catalog unit reading the same entry with opposite polarity
- (decision) High 1, absent scope session defines no opportunity: its hit per session is already the rate, one opportunity per non-empty session
- (question) Should absent gain a trigger key so a turn is an opportunity only when something happened in it (for example an edit before a test run)? An AD-7 vocabulary change; without it every turn is an opportunity for scope turn
- (decision) High 2, AD-11 with AD-4: an opportunity whose followed value is undecided is excluded from opportunities and followed and counted separately as undecided, reported beside them; it is never recorded as not followed. The triple's third value is True, False or None. Row compliance becomes {opportunities, followed, undecided}. Prevents the compliance figure breaking NFR-4
- (assumption) An event whose first (order) is undecided is also counted as undecided rather than dropped silently, extending the maintainer direction from undecided followed to an undecided trigger
- (decision) High 3, AD-9 package data: the shipped fold map and the shipped-id list are Python literals in one module (ruleprobe/contract_data.py), as SECRET_PATTERNS is. Chosen over importlib.resources because a module literal loads through the import system the same way from a directory, a wheel or a zip on sys.path on 3.9, with no loader API, and the import system loads it once per process, so Registry construction adds no file I/O. Trade-off: the data is edited as Python, not YAML; a test checks it is a pure literal. The corpus stays a directory read only by validity and corpus, never at import or registry build; a zip caller sets RULEPROBE_CORPUS or unpacks, as the harness does
- (assumption) The module name ruleprobe/contract_data.py for the fold map and shipped ids
- (question) Add a CI job that builds the wheel and imports it from a zip on sys.path, builds Registry() and calls report_data, as agent-harness loads it (replaces the earlier zip-import question)
- (decision) Medium 2, AD-12: a repeated heading slug in one file takes an ordinal suffix in document order (path#slug, path#slug-2); a collision that remains is a finding. Chosen over a heading-path slug because it keeps the id short and the first occurrence stable
- (decision) Medium 3, AD-6 with AD-13: the .events.jsonl loader takes turn and final as written and never re-derives them; label refuses a session hit (turn:-) and says why, because one event cannot reproduce an absent or order hit
- (assumption) label refuses session hits rather than writing the whole turn window; the one-command route covers tool-use hits only in 0.2
- (decision) Medium 4, AD-13: label refuses to write, and says why, when redaction changes the field the detector matched; the negative would otherwise pass trivially
- (decision) Medium 5: PRD Q7 (minimum 20) and Q8 (root __all__ names outside FR-30) added to Deferred with revisit conditions; AD-11 cites Q7 for the default
- (change) Low 1: AD-1 graph gains cli -> registry (cli.py:26) and validity -> events (validity.py:29). Low 2: AD-1's blanket FR-1 to FR-34 dropped; FR-9 and FR-17 to FR-20 bound in AD-11, FR-9 cited on the Errors convention; NFR-9 stays Deferred. Low 3: AD-14 cites rules.py:53 and :57. Low 4: AD-9 says 0.1.0 ignores a file-level version, and from 0.2 a value that is not a known schema version is a finding. Low 5: AD-9 marks integer as the PRD glossary's working type, with the type and value open under Q9
- (change) PRD amended in the same run: NFR-4 status set to partial (#19); glossary entry Followed corrected for absent to match the code. Logged in the PRD memlog
- (event) Questions resolved this update: fix not and absent over unparsed input in the 0.2 break (answered by the #19 direction); the zip-import CI question is replaced by the wheel-from-zip CI job question. Re-distilled the spine; lint_spine.py 0 findings; no further reviewer lenses run here, the polish lenses run after this update; status left draft
- (change) polish: structure, cut AD-10's PRD Q1 line, which Deferred already carries
- (change) polish: prose, named the antecedent in AD-4 (the undecided rule) and AD-11 (compiles opportunities)
- (change) polish: prose, turned non-status parentheticals in AD-3, AD-4, AD-5, AD-8 and AD-11 into sentences
- (change) polish: prose, grammar fixes in AD-4 (except, both not and absent) and the Deferred split-unit line
- (change) AD-1: detectors.catalog (story 3.5) added to the dependency graph as a Python literal module under AD-9; rules imports it, it imports matchers only (readiness M1, 2026-09-23)
- (change) AD-9: fold map rule now folds a row's compliance map through the same function, summing opportunities, followed and undecided under the current id as hits fold (readiness M2); Catalog bullet added: catalog entries are literals in ruleprobe/detectors/catalog.py, never a data file read at bundle build (readiness M1). lint_spine.py 0 findings; reviewer lenses not run in this update; status left final
- (event) Update intent, headless, per issue #62 (RP-T005): the maintainer's 0.2 decisions of 2026-09-23 recorded; AD ids unchanged, no AD added or retired
- (change) AD-3 names Gemini CLI as the third reader to map (PRD Q1); RP-SP003 lists its file tools exact or native before the reader; assumption reworded to name Gemini CLI
- (change) AD-8 drops the drafting-command boundary and FR-34 from Binds: no command anywhere in ruleprobe calls a model; drafting moved to the judge library tracked in #21 (PRD Q4, RP-D011)
- (change) AD-9 states the integer schema version, 0.2.0 writes 2 (PRD Q9); root __all__ names outside FR-30 stay importable and undeclared (PRD Q8); a later 0.x minor may break with notice, a Breaking changelog heading and the contract test updated in the same change (PRD Q12)
- (change) AD-11 applies the minimum of 20 opportunities per printed group (PRD Q7) and records the keyword-only opportunities callable as decided (PRD Q10); its assumption removed
- (change) AD-12 splits on headings only for 0.2 (PRD Q2), id-form assumption removed; catalog is a small set of six to eight shapes listed in PRD FR-16 (PRD Q3)
- (change) Capability map: FR-34 withdrawn 2026-09-23, moved to #21, not in ruleprobe
- (change) Deferred: third runtime, split unit, catalog size, drafting command, minimum opportunities, root __all__ names, schema version value and later breaks, and promote? wording removed as decided; compliance by position kept as out of 0.2.0, a candidate beyond 0.2
- (event) lint_spine.py --workspace reports 0 findings; reviewer-lens subagents not run (this worker has no subagent tool); status stays final
- (change) AD-4 status: ADOPTED in full; the negation rule implemented under #19 (RP-B001), unreleased, shipping in v0.2.0; the 0.1.0 over-count kept as historical
- (change) AD-8 status: enforcing tests (no dependency, no forbidden import, report writes and sends nothing) implemented under #37 (RP-T002), unreleased, shipping in v0.2.0
- (change) Stack: build backend floor amended to setuptools>=77 for the SPDX licence expression (#6); the dated >=61 row kept as evidence
- (event) Update intent, headless, per issue #36: two AD rules amended in place to match the code on main at 6baa715; AD ids unchanged, no AD added or retired
- (decision) AD-4: a text read of source: heredocs over a command the shell parse skipped is undecided, beside every command key but regex and unparsed, every git key and every env key; the parse is what finds heredoc bodies, so a skipped parse never looked (ruleprobe/matchers.py docstring and texts()). Binds the text matcher and every not, absent and order over it. Prevents a matchers unit reading no bodies as none there, so not or absent over heredocs counts a command nobody could read
- (decision) AD-12: a file bound in its front matter keeps one rule, named by its front-matter rule: key when that is a non-empty slash-free string, else its file name without extension; a rule: value that is not one is a finding and the file name stands (rules.read_rule_file, 0.1.0 and main). Replaces the spine's path-relative-to --rules wording, which no release implemented. Binds rules.py, the report and every detector id prefixed by the rule name. Prevents a rules unit keying file rules by relative path while shipped rows and detector ids key them by name, which would split one rule's history
- (question) A section rule's id (AD-12) carries the rule file's path relative to --rules, which holds / for a file in a subdirectory, while the Consistency Conventions say a rule name holds no slash and rules.py refuses a slashed front-matter name. How does a catalog detector bind such a rule, and is the section id a rule name under that convention? RP-S016 (the catalog story) must settle it
- (question) Two rule files under --rules with the same file name in different subdirectories, or the same front-matter rule: name, get one rule name (AD-12); rules.load_rules_dir neither refuses nor reports it. Should a repeated file-rule name be a finding? Found by the adversarial lens of this update; not settled here
- (event) Update #36 closed: AD-4 and AD-12 rules amended in place, spine updated 2026-09-23, status stays final. Reviewer gate: lint_spine.py 0 findings; rubric, verification and adversarial lenses run inline, not as subagents (this worker has no subagent tool); no spine fix applied from them, one open question logged
- (change) AD-4 and AD-12 wording corrected to the code during review of #36: AD-4 keeps the segment-matcher definition and adds the heredocs text read beside it; AD-12 says rule: with no value is ignored and a rule: key on a file split at its headings is its own does-not-apply finding (rules.read_rule_file)

- (event) Update intent, headless, for story RP-S017 (#51): AD-1 amended in place to sanction explain's two function-local upward imports; AD ids unchanged
- (decision) AD-1 sanctions two more function-local upward imports, both in report's explain helpers and no others: report._hit_key imports validity (hit_key, per AD-6) and report._redact imports detectors.common (redact, per AD-13); five in all, the no-others clause kept, the diagram unchanged because it draws no sanctioned upward import. Binds report's explain path (FR-25). Prevents explain copying the hit-key format or a second redact into report to avoid the upward import, which would let an explained key drift from a label key or let a secret shape bypass SECRET_PATTERNS, and prevents a module-level import that makes a stored-row report load the transcript readers
- (event) AD-1 amendment closed: lint_spine.py --workspace reports 0 findings; rubric, verification and adversarial lenses walked inline (no subagent tool), no finding on the amendment; helper names checked against report.py at 4859ab1, no import cycle; status stays final, updated 2026-09-23
- (decision) AD-11 heading tag: retagged [ADOPTED; implemented under #41, #42 and #43, ships in v0.2.0] once #43 put compliance in the report, the form AD-4, AD-8 and AD-12 use. Binds the tag only; no rule text changed.
- (decision) AD-13 heading tag: retagged [ADOPTED; explain implemented under #51 and label under #52, ship in v0.2.0] once label landed, the form AD-4, AD-8, AD-11 and AD-12 use. Binds the tag only; no rule text changed.

- (event) Update intent, headless, per issue #49 (RP-S016, the shipped catalog). Change signal: the story file's build-time decisions 1 to 8 and the code at 0afbe75 (ruleprobe/detectors/catalog.py, ruleprobe/rules.py, ruleprobe/cli.py). AD ids kept stable; no AD added or retired; validation-report.md and reviews/ not edited
- (decision) AD-1, recording the builder's in-place edit: detectors.catalog holds only literals and imports nothing; rules imports it and compiles each entry once at import with compile_detector; cli reads catalog entries only through rules.catalog_detectors(), never by importing detectors.catalog. The planned detectors.catalog -> matchers edge is dropped; no new edge, since cli -> rules and rules -> matchers exist. Binds every reader of the catalog. Prevents an import in catalog.py breaking its literals-only test and the zip import, and a second reader compiling the literals again or listing an id the shipped fold map retires, which catalog_detectors() filters out
- (decision) AD-12 precedence: the registry is base (DEFAULT, or DEFAULT plus plugins), then the catalog detectors a rule bound (every entry for corpus and detectors), then the loaded detector files in load_bundle's order; a later id replaces an earlier one (Registry.add), except that a catalog entry never replaces an id already held. An entry restating a shipped detector (verification/no-verify, transcript-hygiene/whole-file-cat) leaves the shipped one in place and its rule stays catalog-bound; a plugin or base registry already holding a catalog id wins, and the rule is labelled own, bound to that detector; a user's loaded detector with a catalog id replaces the entry, rule own. Prevents a report carrying two lines for one observable, a catalog entry silently displacing a plugin's detector, and the coverage block naming the catalog for a rule someone else's detector measures
- (decision) AD-12 split unit: a rule file with neither detector nor opt_out that has no heading, or whose headed sections include no rule, stays one rule named by rule: or its file name, and its whole text, headings ignored, binds the catalog as one unit under the same exact-one-match rule. Binds rules.py's section and file binding. Prevents the same sentence binding under a heading and staying unmeasured by construction in a heading-less file, or a builder binding such a file by its first paragraph only
- (decision) AD-14 anchors: cite Bundle.registry and its base.copy() by name, not by line; rules.py:53 and :57 moved when #49 added the catalog code above them. Binds the citation form in AD-14. Prevents a line anchor sending a reviewer to the wrong code each time rules.py grows
- (decision) Coverage catalog count: report --json's coverage block (Bundle.coverage) carries catalog, the count of measured rules whose source is still catalog after Bundle.registry relabels replaced ones own; it is outside the share's denominator. The spine described no coverage shape and the Row convention covers rows, which coverage is not, so the note sits in AD-12 beside the binding source it counts. Prevents a consumer adding catalog into the rule total and double-counting measured rules, or counting catalog bindings before relabelling
- (decision) AD-9 Catalog line: status moved from planned to implemented under #49 (RP-S016), unreleased, shipping in v0.2.0; the catalog ids joined SHIPPED_IDS in contract_data.py in the same change. Binds the status tag only. Prevents a builder treating the catalog as still to be built and adding a second one
- (question) AD-12's heading tag still reads [ADOPTED per file; PROPOSED per section and catalog]; section rules (#78) and the catalog (#49) are now implemented, unreleased. Retag it the way AD-4 and AD-8 are tagged? Left unchanged here: the section status is outside the #49 brief
- (event) Reviewer gate: lint_spine.py 0 findings. The rubric walker, verification and adversarial lenses ran inline against the amended ADs, not as subagents (this worker cannot spawn them) and with no file in reviews/, since the brief confines edits to the spine and this memlog. One fix applied: AD-1 says every other package module, since tests import the catalog directly. Every amendment was checked against the code at 0afbe75. Not acted on, outside these amendments: an id the shipped fold map retires never binds the catalog (in code, not in AD-12); coverage is carried by report --json only, not report_data, beside the Output convention
- (event) Spine amended in place for #49: AD-1, AD-9, AD-12 and AD-14; status stays final; updated 2026-09-23
- (decision) AD-12 heading tag, answering the question above: retagged [ADOPTED; per section implemented under #48 and the catalog under #49, ship in v0.2.0], the form AD-4 and AD-8 use once their pieces are built. Binds the tag only; no rule text changed.
- (correction) The question entry above that reads "section rules (#78)" means issue #48, delivered by PR #78; the entry stands as written.
