# SPDX-License-Identifier: MIT
"""Detectors.

`common` holds the six that ship with the package. Anything about one project's own rules
belongs in that project, registered through the `ruleprobe.detectors` entry point group or
by calling `Registry.add` directly; see `ruleprobe.registry`.
"""
